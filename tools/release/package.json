{
  "name": "@k2/release-tool",
  "version": "1.0.0",
  "private": true,
  "bin": {
    "k2-release": "bin/release.ts"
  },
  "scripts": {
    "build": "echo TODO",
    "test": "echo TODO",
    "clean": "rm -rf dist",
    "lint": "npm run eslint && npm run prettier -- --check",
    "lint:fix": "npm run eslint -- --fix && npm run prettier -- --write",
    "eslint": "eslint --ext .ts src",
    "prettier": "prettier 'src/**/*' '**.json'"
  },
  "dependencies": {
    "@k2/argocd": "^1.0.0",
    "find-workspaces": "^0.3.1"
  },
  "devDependencies": {
    "@k2/build-tools": "^1.0.0"
  }
}
