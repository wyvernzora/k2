# Review ledger — appliance-side backup setup (commits 482359a9, 9bc943b1)

Artifact: `kairos/node-agent` (internal/snapshot, internal/metrics snapshot collector, `snapshot` command),
`kairos/overlays/role/storage` (k2-snapshot-*.service/.timer, 10-storage-services.yaml, overlay.yaml inspect),
`tools/internal/workflow/provision` (backup user, snapshot env), `tools/internal/image` (per-target imageRevision).

Design authority: `docs/design/storage-appliance.private.md` (D26, D31 as amended),
`docs/design/storage-appliance-checkpoint.private.md`.

Rounds so far: 0, 1.

| ID | Location | First cycle | Last cycle | Status | Final severity | Vote summary | Resolution | Reopen policy |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F0-1 | `tools/internal/workflow/provision/storage.go:642-668` (snapshotEnv, snapshotKeep, defaultSnapshotHourlyKeep/DailyKeep); `storage_api.go:5-37` (StorageInputs doc, RunStorage); `provision.go:100-101` kong tags; the two `k2-snapshot-*.service` Environment= lines; `provision_storage_scripts_test.go:181-189` | 0 | 1 | settled | verified | 3/3 verified after the round-0 revision. Checked in live text: `snapshotKeep(keep, fallback)` returns 48/30 for any `keep < 1` and snapshotEnv routes both values through it, so RunStorage's zero-valued path now renders `K2_SNAPSHOT_HOURLY_KEEP=48` / `K2_SNAPSHOT_DAILY_KEEP=30` and can no longer trip `snapshot.Run`'s `keep must be >= 1` guard — the round-0 blocking consequence ("the e2e storage provision step ... cannot produce an appliance with working snapshot cadence ... the units would instead fail on every timer tick") is dismantled. Four independent statements of the defaults agree: consts 48/30, kong `default:"48"`/`"30"`, both unit Environment= lines, design authority :601 "48 hourly + 30 daily". `TestSnapshotEnvDefaultsUnsetRetention` present; `cd tools && go test ./...` exits 0. | verified — no action. The revision took the resolution's normalize-only branch; the unwritten bundle-level assertion stays settled coverage debt under F0-17, and the operator-typed-zero residual is carried separately as F1-2 | Reopen only if snapshotEnv/snapshotKeep, StorageInputs/RunStorage, or the snapshot keep validation is revised in a round >= 1 |
| F0-2 | `kairos/node-agent/internal/snapshot/snapshot.go:58-77` (Run prune loop); `snapshot_test.go` TestRunPrunesRemainingVictimsAfterDestroyFailure | 0 | 1 | settled | verified | 3/3 verified after the round-0 revision. Checked in live text: the early `return` on first failed destroy is gone; every victim is attempted, failures accumulate into `errs`, and `errors.Join(errs...)` is returned after the loop, with a comment naming the designed trigger (a `zfs hold` by the NAS pull identity, a busy clone). Because Run re-enumerates each tick and `prunable` recomputes `sorted[:len-keep]` from scratch, the steady state is keep + permanently-stuck, not the round-0 blocking consequence ("would instead accumulate snapshots indefinitely — one more per tick — from the moment any single victim becomes undestroyable, on the 7.7T pool that holds every live PVC"). The added test forces a destroy failure on the oldest of four victims with Keep=1 and asserts both the reported failure and that the two later victims were still destroyed. `cd kairos/node-agent && go test ./...` exits 0. | verified — no action | Reopen only if the prune loop, the destroy invocation, or the backup sudoers hold grant changes in a round >= 1 |
| F0-3 | `kairos/node-agent/internal/snapshot/snapshot.go`, listPrefixSnapshots doc comment and `zfs list ... -d 1 <dataset>` | 0 | 0 | open | defer | 3/3 defer. Facts hold (`-d 1` never enumerates child snapshots; the comment asserts recursive-destroy atomicity that zfs-destroy(8) does not promise, only `zfs snapshot -r` is documented atomic), but the harm requires an unproven partial-recursive-destroy: OpenZFS validates all gathered snapshots in the check phase before the sync task, so an EBUSY child normally aborts the whole destroy rather than orphaning the parent. Future cost: an overclaiming comment plus no reconciliation path for descendants. | defer — correct the comment to claim atomicity only for create; consider a reconciliation/`zfs list -r` sweep if partial destroy is ever observed | Reopen only if listPrefixSnapshots scoping or the destroy path changes in a round >= 0 |
| F0-4 | `tools/internal/workflow/provision/storage.go:638-640` backupSudoers doc comment; `tools/internal/render/render.go` BackupUserActivationCloudConfig comment | 0 | 0 | open | defer | 3/3 defer. Both halves verified: the grant includes `zfs hold`/`release`, which mutate snapshot state and can block the appliance's own destroys, while both comments call the grant "read-side". The grant itself matches the design authority verbatim (F0-21), so nothing built differs; the residual is "prose that will mislead a later reader about the hold-vs-prune interaction". The operational consequence is carried as blocking under F0-2. | defer — reword the comments to say "non-destructive to datasets" and name the hold/prune interaction explicitly | Reopen only if the sudoers const or its comments change in a round >= 0 |
| F0-5 | `kairos/node-agent/internal/metrics/metrics.go`, collectZFSSnapshots + Render | 0 | 0 | open | defer | 3/3 defer. Verified: `groupResult{success: true}` is set before the parse loop and samples append only inside the per-prefix loop, so an empty byPrefix emits zero series while `k2_collector_success{collector="zfs_snapshots"} 1` still renders — absence, not staleness, is the signal, and `time() - k2_zfs_last_snapshot_timestamp_seconds > threshold` cannot fire. No k2_zfs_* alert rules exist anywhere in the repo, so the stopped consumer is the future alert author. | defer — emit a zero/absent-safe series (or an expected-keep gauge) before the D31 snapshot-age alert is written; `absent_over_time()` is the workaround until then | Reopen only if collectZFSSnapshots, Render, or any k2_zfs_* alert rule lands in a round >= 0 |
| F0-6 | `kairos/node-agent/internal/metrics/metrics.go:551-560`, cadenceSnapshotPattern + collectZFSSnapshots | 0 | 0 | open | defer | 3/3 defer. Verified: `zfs list -Hp -t snapshot -o name,creation` takes no dataset and no depth, so it walks every snapshot on every imported pool, and the prefix label is arbitrary text before the trailing UTC stamp; the comment's "manual and migration snapshots never match" is false for any stamp-suffixed name — likely during the Longhorn-to-iSCSI migration. The k2-hourly/k2-daily series themselves are correct today; a stray prefix creates a separate series rather than corrupting a real one. Note the asymmetry: the prune path is properly scoped with `-d 1 <dataset>`. | defer — scope the collector to K2_SNAPSHOT_DATASET or filter to the two cadence prefixes; fix the comment | Reopen only if collectZFSSnapshots' zfs invocation or the regex changes in a round >= 0 |
| F0-7 | `kairos/node-agent/internal/metrics/metrics_test.go:303-337` (TestCollectZFSSnapshots, TestCollectZFSSnapshotsMalformedLine) vs `metrics.go:556-612` | 0 | 1 | settled | verified | 3/3 verified after the post-round fix. Checked in live text: TestCollectZFSSnapshots supplies a parent row and a recursive-child row sharing one suffix and asserts hourly count 2 (not 3), per-prefix latest creation, daily count 1, and `assertNoSample` for the non-cadence `@migrate`; TestCollectZFSSnapshotsMalformedLine asserts `k2_collector_success` 0 after a malformed row while the valid row's series still emits. Both match the implementation's actual branches (success=false + continue on bad field count / missing `@` / unparsable creation; samples appended after the loop regardless). The round-0 defer ("the 70-line collector added by 482359a9 ships with no test") is discharged, and the round-0 observation that an earlier test append had silently failed is confirmed accurate. | verified — no action. Residual concerns about what the new fixtures pin are carried as F1-3 and F1-6 | Reopen only if metrics_test.go or collectZFSSnapshots changes in a round >= 1 |
| F0-8 | `kairos/overlays/role/storage/oci/etc/systemd/system/k2-snapshot-{hourly,daily}.service` | 0 | 0 | open | defer | 3/3 defer. Verified: `EnvironmentFile=-/oem/k2-snapshot.env` is optional and the Environment= defaults hardcode `tank/csi/k2` + 48/30. On the deployed appliance those defaults are exactly correct (F0-24 verifies this is the deliberate upgrade-activation mechanism); on a different pool/cluster the baked dataset does not exist and the unit fails loudly. The genuinely silent branch needs /oem absent, which would also strip the D26 key file and block pool import. Residual: hardcoded pool+cluster name in a role overlay, and no OnFailure=. | defer — log the effective dataset/keep at start, and derive or guard the baked dataset for a second-cluster appliance | Reopen only if either service unit's Environment/EnvironmentFile lines change in a round >= 0 |
| F0-9 | `kairos/overlays/role/storage/oci/etc/systemd/system/k2-snapshot-{hourly,daily}.service` [Unit] vs `k2-zfs-load-key.service` | 0 | 0 | open | advisory | 2 advisory, 1 defer → advisory. Structural facts hold (k2-zfs-load-key reaches zfs.target only via [Install] WantedBy=, After= binds nothing across a fresh timer transaction, Requires=zfs.target is stricter than sibling units). But nothing predictable is lost: `zfs snapshot` succeeds on a dataset whose key is not loaded (snapshotting reads no plaintext), and a genuinely un-imported pool fails the create immediately and visibly. Missing OnFailure= has no notification target in the image to point at. | advisory — no action; revisit if a failure-notification target is added | Reopen only if the snapshot units' [Unit] section or k2-zfs-load-key's install wiring changes in a round >= 0 |
| F0-10 | `docs/design/storage-appliance.private.md:105` (D31 row) and `:567-619` (§8 Backups); roadmap item 8 (~:865) | 0 | 1 | settled | verified | 3/3 verified after the round-0 rewrite. Checked in live text (the doc is gitignored via `.gitignore:91:*.private.*`, which is why the rewrite is absent from git diff): D31 and §8 now specify recursive snapshots of the CSI parent dataset `tank/csi/<cluster>` "not of the encryption root", k2-hourly/k2-daily via `k2-node-agent snapshot`, source-side pruning "even when the NAS never pulls", `/oem/k2-snapshot.env` rendered by `k2-tools provision storage` with `--snapshot-hourly-keep`/`--snapshot-daily-keep`, a **non-raw** incremental pull re-encrypted into a rumi-owned encryption root (`data/backup/k2`, middleware hex key, auto-unlock), `properties=false`, `special_small_blocks=0`, the 2026-08-09 threat-model note, 48+30 retention, restore-drill requirements, and an explicit list of what the stream does not carry (tank / tank/csi properties, the `tank/csi/<cluster>-snapshots` sibling). The stated grant matches `backupSudoers` (storage.go:640) verbatim. The round-0 blocking consequence ("the implementer ... cannot build the correct pull from the primary design doc because D31 and §8 still specify raw `zfs send -wR` ... would instead build raw replication of tank") no longer holds. Roadmap item 8 still phrases the shipped timers as future work — disclosed as deliberate, carries no contradicting raw-send language. | verified — no action. The checkpoint's own stale DECISION row is carried separately as F1-1 | Reopen only if D31/§8 or the snapshot dataset scope changes in a round >= 1 |
| F0-11 | `kairos/overlays/role/storage` timers vs `docs/design/storage-appliance-checkpoint.private.md:396`; `tools/internal/workflow/e2e/run.go` storage checks | 0 | 0 | open | defer | 3/3 defer. Verified timing: the clean-teardown smoke test (~18:0x) predates 482359a9 (23:27 same day), so democratic-csi's delete path has never met zvols carrying cadence snapshots, and k2-iscsi uses detachedSnapshots=false (native snapshots/clones inside the same parent). Coverage not yet written: the operator's next action (deploy rev2, enable timers) succeeds, and a broken DeleteVolume would leave the PV in Terminating — loud, blocking a later action, not this one. Second cost: space held by cadence snapshots of already-deleted zvols for up to the 30-day daily window. | defer — re-run the PVC create/snapshot/restore/delete smoke with the timers active before the Longhorn migration; consider an e2e assertion | Reopen only if the storage e2e steps, the timers, or democratic-csi's delete configuration change in a round >= 0 |
| F0-12 | `tools/internal/image/config/config.go:27-42` (Target) and `:208-222` (rejectUnknownTargetKeys); `tools/internal/image/plan/plan.go:195,419-438`; `kairos/targets.yaml` storage entries; `plan_test.go:726-759` | 0 | 1 | settled | verified | 3/3 verified after the user-ratified round-0 deletion. Checked in live text: `config.Target` has no image-revision field and the allowed target-key set omits `imageRevision`; resolveTarget no longer merges it; `plan.go:195` assigns the global `Versions: p.Versions` (grep for `Versions:` in plan.go returns only :124 and :195, i.e. no per-target copy); `imageTag` is a Planner method deriving the suffix solely from `p.Versions.ImageRevision` with a rationale comment forbidding per-target overrides; both storage entries in targets.yaml have their pins removed; repo-wide grep finds no target-level imageRevision anywhere. `TestAllTargetsUseGlobalImageRevision` builds every enabled target from the real targets.yaml+versions.env and asserts the global suffix. The round-0 blocking consequence ("the maintainer ... cannot advance it to rev2 ... would instead re-push the already-published storage rev2 tags with different content") is structurally impossible now. `cd tools && go test ./...` exits 0. | verified — no action | Reopen only if per-target revision handling or the global imageTag derivation changes in a round >= 1 |
| F0-13 | `tools/internal/image/plan/plan.go` (formerly :200 per-target `Versions: versions`, now :195 `Versions: p.Versions`); `kairos/versions.env`; `kairos/targets.yaml`; `tools/internal/step/image/build_oci.go:92-93`; `tools/internal/step/upgrade/resolve.go:381-393` | 0 | 1 | settled | verified | 2 verified, 1 defer → verified. Proactively reopened by the collector because the round-0 revision rewrote this row's locations, making its round-0 certification stale: all three things it recorded are now false of the tree (the per-target copy at :200 is gone, `TestPerTargetImageRevisionOverride` was replaced by `TestAllTargetsUseGlobalImageRevision`, and the net arithmetic is now every-target rev1 -> rev2 rather than "k8s unchanged at rev1, storage advancing to rev2"). Re-checked this round: the two downstream consumers it also certified — build_oci.go's `--build-arg VERSION`/`IMAGE_REVISION` and upgrade-side `imageRefFromMetadata` — were not edited and now read the single global value on every target, which is correct end to end. The dissenting defer wanted the row marked superseded rather than re-verified; the substance is identical. | verified — no action beyond this row's own correction. The current state is what F0-12 and F0-23 verify; do not carry the round-0 wording forward | Reopen only if the global revision plumbing or its downstream consumers change in a round >= 1 |
| F0-14 | `tools/internal/workflow/provision/storage.go:649-657` (snapshotEnv, edited by the round-0 revision); `kairos/node-agent/internal/snapshot/snapshot.go` listPrefixSnapshots/prefix filtering; the four k2-snapshot unit files | 0 | 1 | settled | verified | 3/3 verified. Proactively reopened by the collector because snapshotEnv — one of this row's locations — was edited. Re-checked: the three emitted variable names (K2_SNAPSHOT_DATASET, K2_SNAPSHOT_HOURLY_KEEP, K2_SNAPSHOT_DAILY_KEEP) are still exactly the three the units consume via Environment= + `EnvironmentFile=-/oem/k2-snapshot.env` + `${...}` interpolation in ExecStart; the dataset-derivation line `flags.Pool + "/csi/" + clusterNameOrFallback(...)` is byte-identical to what round 0 verified; only the two keep expressions changed, now wrapped in snapshotKeep. Prefix filtering (mutually non-prefixing k2-hourly/k2-daily) and the `-d 1` listing were not touched. All four couplings survive the edit. | verified — no action | Reopen only if prefix filtering, snapshotEnv's variable names, or the unit ExecStart lines change in a round >= 1 |
| F0-15 | `tools/internal/workflow/provision/storage.go:649-650` (snapshotEnv dataset derivation, clusterNameOrFallback); `provision_storage_scripts_test.go:181-189` | 0 | 1 | open | advisory | 3/3 advisory. Proactively reopened by the collector because both locations were edited. Re-checked: the derivation line is unchanged, and the new `TestSnapshotEnvDefaultsUnsetRetention` passes `ClusterName: "k2"` explicitly, so it exercises the non-fallback branch — clusterNameOrFallback's fallback path inside snapshotEnv remains unreached by any test and, per the settled analysis (both callers normalize ClusterName to ClusterTarget before buildStorageBundle), unreachable in production. Inert defensive code; nothing predictable is lost by leaving it untested. Severity unchanged by the revision. | advisory — no action | Reopen only if ClusterName normalization or snapshotEnv's dataset derivation changes in a round >= 1 |
| F0-16 | `kairos/overlays/role/storage/overlay.yaml` inspect structuredTests on `/system/oem/10-storage-services.yaml` and the oci.files entries | 0 | 0 | open | defer | 3/3 defer. Verified: the new timers sit at enable indices 8 and 9, while structuredTests assert only enable/0, enable/1 and enable/5 — so an image that ships both unit files but drops them from the enable list passes inspection with a green build and dead timers. Per-file checks are asymmetric: hourly is checked for EnvironmentFile= and ExecStart, and its timer for OnCalendar plus Persistent=true; daily is checked only for ExecStart and OnCalendar, though both daily files contain the unasserted lines. Today's image is correct (F0-24), so nothing is stopped. | defer — add enable-list assertions for both timers (prefer value-match over index) and level up the daily-unit checks | Reopen only if overlay.yaml inspect tests or the 10-storage-services.yaml enable list changes in a round >= 0 |
| F0-17 | `tools/internal/render/` (no backup activation test); `provision_storage_test.go:512` TestBuildStorageBundleGoldenStyle | 0 | 0 | open | defer | 3/3 defer. Verified: `render.BackupUserActivationCloudConfig` ships with no test although sibling CSIUserActivationCloudConfig has `csi_activation_test.go`; TestBuildStorageBundleGoldenStyle supplies no backup keys, so the `len(backupKeys) > 0` branch at `storage.go:614-618` (BackupKeys/BackupSudoers/BackupActivation) is never exercised end to end, and the test asserts nothing about k2-snapshot.env, 94-k2-storage-backup.yaml, 98-backup or backup_authorized_keys. Coverage not yet written on the security-sensitive path: sudoers content and the 0440 root/root permissions can drift undetected. | defer — add a backup activation render test and extend the bundle golden test with backup keys | Reopen only if render.go's backup activation or the storage bundle backup branch changes in a round >= 0 |
| F0-18 | `tools/internal/workflow/provision/storage.go:539-553` (plan confirmation table) | 0 | 0 | open | defer | 1 blocking, 2 defer → defer. Verified: the confirmation KVs cover cluster/SSH/image/node/pool/encryption/dataset parent/detached snapshots/CSI/CHAP/hardening/reboot — no retention row and no backup-user row, while SnapshotEnv and the BackupKeys/BackupSudoers/BackupActivation trio are populated after the table is built. Majority: the operator supplied the flags moments earlier and nothing they do differs, so they are not stopped. Predictable future cost the dissent named: "the confirm gate — whose entire purpose is pre-apply visibility — silently does not cover the two new provisioned behaviors, so a retention typo or unexpected backup-key set sails through unreviewed" (and would have surfaced F0-1). | defer — add Snapshot retention and Backup user rows to the plan table | Reopen only if the plan confirmation table or the snapshot/backup bundle members change in a round >= 0 |
| F0-19 | `kairos/node-agent/internal/runner/runner.go` OSRunner.Output; `snapshot.go` listPrefixSnapshots error wrap | 0 | 0 | open | defer | 2 defer, 1 advisory → defer. Verified: `OSRunner.Output` uses `exec.Command(...).Output()` with no Stderr wiring while `OSRunner.Run` sets `cmd.Stderr = os.Stderr`, and listPrefixSnapshots wraps only the exit status — so a failed `zfs list` logs `exit status 1` with ZFS's diagnostic dropped, exactly on the cases that matter (pool not imported, dataset missing, the F0-2 hold scenario). Diagnostics only; the unit failure is still loud and reproducible by hand. Pre-existing OSRunner behaviour the new package inherits. | defer — capture stderr in OSRunner.Output (or unwrap *exec.ExitError.Stderr at the call site) | Reopen only if OSRunner.Output or the listPrefixSnapshots error path changes in a round >= 0 |
| F0-20 | k2-snapshot-*.service; `provision.go` snapshot keep defaults; `snapshot.go:40-60` | 0 | 0 | settled | verified | 3/3 verified against checkpoint:397 ("k2-hourly-*×48 + k2-daily-*×30 recursive on tank/csi/k2"): unit Environment= defaults (tank/csi/k2, 48, 30), kong defaults 48/30, and rendered /oem/k2-snapshot.env all agree; keep semantics confirmed — Run() creates before listing and prunable returns nil while len(sorted) <= keep, so keep=N retains N total including the snapshot just created. Both `go test ./...` suites pass (2026-08-10). | verified — no action. Conformance to the *amended* parameters only; the stale primary doc is F0-10 | Reopen only if the cadence parameters or Run()'s create/prune ordering change in a round >= 0 |
| F0-21 | `tools/internal/workflow/provision/storage.go:640` (backupSudoers const) | 0 | 0 | settled | verified | 3/3 verified against storage-appliance.private.md:582-585 ("only the ZFS inspection/send/hold/release operations required by the replication engine—no snapshot creation, receive, rollback, destroy, or shell administration"): the const grants exactly send, list, get, hold, release and nothing destructive; every destroy on the appliance is issued by the timers' own `zfs destroy -r`. The "read-side" comment imprecision is carved out to F0-4. | verified — no action | Reopen only if backupSudoers or the design doc's stated grant changes in a round >= 0 |
| F0-22 | `tools/internal/workflow/provision/storage.go:640` (sudoers argument patterns) | 0 | 0 | open | advisory | 1 verified, 2 advisory → advisory. Three accurate sudoers facts, none changing what anyone builds: wildcards span whitespace but each pattern still pins argv[0] to the named subcommand (no escape past /usr/sbin/zfs); the literal space denies a zero-argument `sudo zfs list`, a quirk the replication engine never hits; and no pattern scopes the dataset, which checkpoint:397 explicitly settles ("'rumi reads k8s data' was never a guarded property"). The /usr/sbin/zfs path is corroborated by k2-zfs-load-key.service's own ExecStart on the appliance whose boot proof passed. | advisory — no action; useful context for whoever writes the NAS pull task (always pass arguments) | Reopen only if the sudoers patterns or the threat model change in a round >= 0 |
| F0-23 | `kairos/versions.env:12-21`; `tools/internal/image/plan/plan.go:419-438`; `kairos/scripts/resolve-ci-targets.sh:32-52` | 0 | 1 | settled | verified | 3/3 verified after the round-0 revision. Checked in live text: versions.env carries a single global `K2_IMAGE_REVISION=rev2` preceded by a comment stating the global-on-purpose invariant, and imageTag appends that value to every resolved target tag. Net effect versus the last-published state: all targets move rev1 -> rev2, so no already-published tag is re-pushed with different node-agent content — the round-0 blocking consequence ("CI silently overwrite[s] the existing rev1 tag with a materially different node-agent binary ... with no build error, no new tag, and no signal to any consumer", while imageRefsMatch compares tags not digests) is resolved. The comment's factual claim about CI holds: resolve-ci-targets.sh's `common_input_changed` arms match the workflow file, kairos/Earthfile, targets.yaml, versions.env, kairos/Dockerfile, tools/*, kairos/scripts/*, kairos/node-agent/*, including the Dockerfile the comment names. | verified — no action | Reopen only if versions.env/targets.yaml revisions, the CI target-resolution inputs, or the upgrade up-to-date check change in a round >= 1 |
| F0-24 | k2-snapshot-*.service Environment/EnvironmentFile ordering; `10-storage-services.yaml` initramfs enable stage | 0 | 0 | settled | verified | 3/3 verified. Env-file precedence checked in live text: both Environment= assignments precede `EnvironmentFile=-/oem/k2-snapshot.env`, and systemd.exec(5) applies assignments in order with later winning, so a rendered env overrides the baked defaults while the '-' prefix tolerates absence and a partial file leaves omitted keys at their Environment= default. The baked defaults match the deployed k2-st-0e12 exactly (checkpoint:391, datasets tank/csi/{k2,k2-snapshots}), and the initramfs enable stage (guarded by `[ ! -f /run/cos/recovery_mode ]`) re-runs each boot, so upgrading to the rev2 image activates the cadence with correct parameters and no re-provision. | verified — no action. Same mechanism as F0-8, read from the opposite end; both readings are correct for their scenarios | Reopen only if the unit env ordering or the initramfs enable stage changes in a round >= 0 |
| F1-1 | `docs/design/storage-appliance-checkpoint.private.md:220-223` (DECISION: D31 backup …) and `:330-331` (residual annotation), cross-read against `:397` and `docs/design/storage-appliance.private.md:105` / `:567-596` | 1 | 1 | open | defer | 3/3 defer. Fact confirmed: checkpoint:220-223 still records D31 as "TrueNAS pull of raw encrypted incrementals ... destination normally locked/read-only. Ref: `docs/design/storage-appliance.private.md` D31 + §8", and the residual annotation at :330-331 ("← D31 raw TrueNAS pull with 48h+30d") is likewise unstruck, while the referenced passages now say non-raw, re-encrypted into a rumi-owned auto-unlocking encroot; the file's own `~~SUPERSEDED: …~~` convention (used at :330) was not applied. No voter could complete the blocking sentence: "the same file records the amendment explicitly at :397 and the primary authority the stale entry itself cites now reads non-raw throughout, so the NAS-pull implementer builds correctly from the authorities the record points at." Predictable future cost: "prose that will mislead a later reader" — a partial reader or a checkpoint-groom pass taking :220-223 as live and drafting the raw/locked design before being corrected. | defer — strike/annotate :220-223 and the :330-331 residual per the file's own SUPERSEDED convention at the next groom | Reopen only if the checkpoint's D31 records or the amended §8 change in a round >= 1 |
| F1-2 | `tools/internal/workflow/provision/storage.go:659-668` (snapshotKeep) and `:649-657` (snapshotEnv); flags at `provision.go:100-101`; plan confirmation table at `storage.go:539-553` | 1 | 1 | open | defer | 1 blocking, 2 defer → defer. Fact confirmed by all three: kong tags are plain ints with no min/range validator, so an operator-typed `--snapshot-hourly-keep=0` or `-5` hits snapshotKeep's `keep < 1` condition and silently renders 48 (daily 30) with no error, warning, log line, or plan-table row (F0-18 settled the missing row). The blocking vote's consequence: "An operator running `k2-tools provision storage` with `--snapshot-hourly-keep=0` ... cannot get the value they typed ... and would instead get 48 hourly snapshots retained and consuming unplanned pool capacity, discovering the mismatch only later by manually inspecting /oem/k2-snapshot.env or noticing unexpected disk usage." Majority declined promotion: 0/negative were never a supported way to disable a cadence (the timers are baked into the overlay and keep=N includes the just-created snapshot; a rendered 0 previously produced a unit that failed every tick, not a disabled cadence), the substitution's only direction is toward the design's own default, provisioning succeeds and the appliance behaves per design, and the outcome is visible in `/oem/k2-snapshot.env` and fully reversible. | defer — reject `keep < 1` at the flag boundary (or distinguish unset from zero with a pointer/sentinel) so the clamp serves only programmatic zero-values, and add the retention row F0-18 already books | Reopen only if snapshotKeep, the kong retention flags, or the plan confirmation table changes in a round >= 1 |
| F1-3 | `kairos/node-agent/internal/metrics/metrics_test.go:326-337` (TestCollectZFSSnapshotsMalformedLine) and `:303-324` (TestCollectZFSSnapshots) | 1 | 1 | open | defer | 3/3 defer. Both facts confirmed: the malformed-line fixture's only well-formed row is `tank@k2-hourly-20260809T180000Z` — a pool-root snapshot outside the cadence dataset — and the test asserts `snapshotCount{prefix="k2-hourly"} == 1` from it, so the new test encodes the collector's unscoped cross-pool counting (metrics.go:560 invokes `zfs list -Hp -t snapshot -o name,creation` with neither dataset nor depth) as expected behavior; and neither new test exercises a stamp-suffixed non-cadence name, the exact case metrics.go:551-553's comment wrongly rules out. Nobody is stopped: the assertions are correct for today's collector and both suites pass. Predictable future cost the voters named: "whoever acts on the settled F0-6 defer (scope the collector to K2_SNAPSHOT_DATASET or filter to the two cadence prefixes) will find this fixture failing and must decide whether it was an intentional contract or an incidental choice." | defer — move the malformed-row fixture's valid row under tank/csi/k2 and add the stamp-suffixed non-cadence case, alongside the F0-6 scoping fix | Reopen only if collectZFSSnapshots' scoping/regex or these two tests change in a round >= 1 |
| F1-4 | `docs/design/storage-appliance.private.md` §8 Backups, `:573-574` and `:592-595` | 1 | 1 | open | defer | 3/3 defer. Fact confirmed: §8 asserts "retention is enforced even when the NAS never pulls" and grants the backup identity "inspection/send/hold/release", but a search of §8 and the D31 row finds `hold` only in those grant lists — no sentence covers a snapshot the engine still holds falling outside the keep count, who releases a stale hold, or how an operator learns one exists, even though the code added by the same revision (snapshot.go:58-61) names precisely that case. No consumer is stopped: the pull implementer configures the replication task identically either way (TrueNAS manages its own holds), and the operator's discovery path is a failed oneshot with `destroy failed for <victim>` in the journal. Predictable future cost: "a designed-for interaction that exists in code but not in the design authority, with no recorded owner for releasing a stale hold" — the next person to hit it re-derives it from source. | defer — add a §8 paragraph stating that a held victim is retained past its tier, the destroy is retried each tick, the timer keeps reporting failure, and hold release belongs to the NAS side | Reopen only if §8's retention/grant text or the prune-failure handling changes in a round >= 1 |
| F1-5 | `kairos/node-agent/internal/snapshot/snapshot.go:72-77` (Run tail); `cmd/k2-node-agent/main.go:48-54`; both `k2-snapshot-*.service` files | 1 | 1 | open | advisory | 1 blocking, 2 advisory → advisory. Facts confirmed by all three: a run where the create succeeded but one prune was blocked returns errors.Join, main.go prints and exits 1, and the oneshot enters failed state identically to a run where nothing was created; `logf("done")` is skipped; grep finds no `OnFailure=` in either unit; the collector registers only `k2_zfs_last_snapshot_timestamp_seconds` and `k2_zfs_snapshot_count`. The blocking vote's consequence — "an on-call operator ... would instead treat every failed unit alike, either escalating a benign self-healing prune block as a full backup outage or ... dismissing a real create failure as 'the known benign prune issue'" — did not persuade: the journal does differentiate (partial path emits one `destroy failed for <victim>` line per stuck victim and no create error), and the metrics differentiate too (count climbs past keep while last-timestamp stays fresh — the shape a future alert keys on). A distinct exit code buys nothing without a notification target, whose absence is settled as F0-9, and the missing alert rules are settled as F0-5. | advisory — no action; revisit when the alerting layer / a failure-notification target lands | Reopen only if Run's error tail, main.go's exit handling, or the units' [Unit] failure wiring changes in a round >= 1 |
| F1-6 | `kairos/node-agent/internal/metrics/metrics_test.go:305-311` (TestCollectZFSSnapshots fixture rows) | 1 | 1 | open | advisory | 3/3 advisory. Fact confirmed by direct measurement this session: `date -u -r 1754758800` = Sat Aug 9 17:00:00 UTC **2025**, `-r 1754762400` = 18:00:00 UTC 2025, `-r 1754697600` = 00:00:00 UTC 2025, against fixture names stamped `20260809T170000Z` / `T180000Z` / `T000000Z` — a uniform one-year offset, so the fixture does not describe data ZFS could produce. Nothing predictable is lost: the collector derives the dedup key from the regex capture and the gauge value from `parseFloat(fields[1])` independently and never cross-checks them, so the test exercises the real paths and passes for the right reasons. | advisory — optional cleanup: shift the three epochs to 1786302000 / 1786305600 / 1786233600 | Reopen only if the collector begins cross-checking the name stamp against `creation`, or these fixtures change, in a round >= 1 |
| F1-7 | `tools/internal/workflow/provision/storage_api.go:5-24` (StorageInputs + doc comment) and `:26-37` (RunStorage) | 1 | 1 | open | defer | 2 defer, 1 advisory → defer. Fact confirmed: StorageInputs' fields contain no SnapshotHourly/SnapshotDaily and RunStorage's commonStorageFlags literal omits them, so every programmatic provision renders 48/30 with no way to express anything else, while the new doc comment ("Fields it omits take the same defaults the CLI flags carry — snapshot retention included, via snapshotKeep") reads as though retention were an omittable field rather than an absent one. No consumer is stopped — the only RunStorage caller is the e2e workflow, for which 48/30 matches the deployed k2-st-0e12 exactly, and the F0-1 resolution's "and/or" explicitly sanctioned this normalize-only path. Predictable future cost the majority named: "a doc comment that sends a later reader hunting for fields that do not exist, plus an API that cannot express a parameter the CLI can." The advisory dissent noted a future caller hits it at compile time — loud and immediate. | defer — either add the two fields (the F0-1 resolution's first branch) or reword the comment to say retention is not settable through this entry point and always takes 48/30 | Reopen only if StorageInputs, RunStorage's flag literal, or that doc comment changes in a round >= 1 |
| F1-8 | `kairos/node-agent` and `tools` Go modules (both requested test suites) | 1 | 1 | settled | verified | 3/3 verified, independently re-run this session by two voters. `cd kairos/node-agent && go test ./...` exits 0 (ok for cmd/k2-node-agent, internal/health, internal/metrics, internal/snapshot, internal/storage; internal/runner has no test files) and `cd tools && go test ./...` exits 0 across all packages including internal/image/plan, internal/step/image, internal/step/upgrade, internal/render, internal/workflow/provision. Uncached `-count=1` runs of every changed package also pass. Affirmative readiness result against the working tree containing the round-0 revision, not a defect. | verified — no action | Reopen only if either suite is re-run against a changed tree in a round >= 1 |

## Round 0 tally

- critical: 0
- blocking: 5 (F0-1, F0-2, F0-10, F0-12, F0-23)
- defer: 11 (F0-3, F0-4, F0-5, F0-6, F0-7, F0-8, F0-11, F0-16, F0-17, F0-18, F0-19)
- advisory: 3 (F0-9, F0-15, F0-22)
- verified (not defects): 5 (F0-13, F0-14, F0-20, F0-21, F0-24)

critical + blocking = 5


## Round 1 tally

Round 1 re-voted the round-0 revision (F0-1, F0-2, F0-7, F0-10, F0-12, F0-23 reopened by the change-log;
F0-13, F0-14, F0-15 proactively reopened because the revision rewrote their locations) and raised F1-1..F1-8.

- critical: 0
- blocking: 0
- defer: 5 (F1-1, F1-2, F1-3, F1-4, F1-7)
- advisory: 3 (F0-15, F1-5, F1-6)
- verified (not defects): 9 (F0-1, F0-2, F0-7, F0-10, F0-12, F0-13, F0-14, F0-23, F1-8)

critical + blocking = 0

All five round-0 blocking findings (F0-1, F0-2, F0-10, F0-12, F0-23) are re-checked in live text and
resolved verified this round. Nothing raised in round 1 reached blocking: the closest, F1-2 (silent clamp
of an operator-typed non-positive retention) and F1-5 (partial-prune indistinguishable from total failure),
were both voted down because the consumer still provisions/operates correctly and the true state is
recoverable from `/oem/k2-snapshot.env`, the journal, and the cadence gauges respectively.
