{
  "name": "@k2/cert-manager",
  "version": "1.0.0",
  "private": true,
  "exports": {
    "./manifest": "./dist/manifest.k8s.yaml"
  },
  "scripts": {
    "build": "mkdir -p dist && helm dependency build && helm template --release-name cert-manager --namespace k2-core . > dist/manifest.k8s.yaml"
  }
}
