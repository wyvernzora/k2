{
  "name": "@k2/1password",
  "version": "1.0.0",
  "private": true,
  "exports": {
    "./manifest": "./dist/manifest.k8s.yaml"
  },
  "scripts": {
    "build": "mkdir -p dist && helm dependency build && helm template --release-name 1password --namespace k2-core . > dist/manifest.k8s.yaml"
  }
}
