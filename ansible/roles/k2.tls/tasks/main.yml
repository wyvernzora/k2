---
- name: Validate TLS proxy configuration
  assert:
    that:
      - k2_pve_management_address is defined
      - k2_pve_management_address | length > 0
    fail_msg: k2_pve_management_address must be set for every Proxmox host.

- import_tasks: cert-sync.yml
- import_tasks: pve.yml
