{
  "name": "@k2/traefik",
  "version": "1.0.0",
  "private": true,
  "exports": {
    "./manifest": "./dist/manifest.k8s.yaml"
  },
  "scripts": {
    "build": "mkdir -p dist && helm dependency build && helm template --release-name traefik --namespace k2-network . > dist/manifest.k8s.yaml"
  }
}
