# Base Overlay

Shared Kairos image content that every K2 target must carry.

Currently this only ships the active-boot `k2-rescue` deactivation config. Keep
hardware behavior and role-specific services in their own overlays.
