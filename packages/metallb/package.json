{
  "name": "@k2/metallb",
  "version": "1.0.0",
  "private": true,
  "exports": {
    "./manifest": "./dist/manifest.k8s.yaml"
  },
  "scripts": {
    "build": "mkdir -p dist && helm dependency build && helm template --release-name metallb --namespace k2-network . > dist/manifest.k8s.yaml"
  }
}
