{
  "name": "@k2/crds",
  "version": "1.0.0",
  "private": true,
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./*": "./dist/*.ts",
    "./app.k8s.yaml": "./dist/manifest.k8s.yaml"
  },
  "scripts": {
    "build": "k2-synth-crds",
    "test": "echo TODO",
    "clean": "rm -rf dist",
    "lint": "npm run prettier -- --check",
    "lint:fix": "npm run prettier -- --write",
    "prettier": "prettier '**.{json,ya?ml}' '!dist/**'"
  },
  "dependencies": {
    "cdk8s": "^2.68.22"
  },
  "devDependencies": {
    "prettier": "^3.2.4",
    "@k2/build-tools": "^1.0.0"
  }
}
