{
  "name": "@k2/blocky",
  "version": "1.0.0",
  "private": true,
  "exports": {
    "./manifest": "./dist/app.k8s.yaml"
  },
  "scripts": {
    "build": "k2-synth-cdk8s",
    "test": "echo TODO",
    "clean": "rm -rf dist",
    "lint": "npm run eslint && npm run prettier -- --check",
    "lint:fix": "npm run eslint -- --fix && npm run prettier -- --write",
    "eslint": "eslint --ext .ts src",
    "prettier": "prettier 'src/**/*' '**.json'"
  },
  "dependencies": {
    "@k2/cdk-lib": "^1.0.0"
  },
  "devDependencies": {
    "@k2/build-tools": "^1.0.0"
  }
}
