# Review ledger — node identity/upgrade metrics, appliance scraping, storage dashboard (uncommitted working tree)

Artifact: `kairos/node-agent` (internal/metrics multi-label renderer + k2_node_build_info, internal/imagecheck,
internal/textfile, internal/buildmetadata, `image-check` subcommand), `kairos/overlays/role/{k8s,storage}`
(k2-metrics/k2-image-check units + timers, cloud-config enable stages, overlay inspect assertions),
`apps/prometheus` (node-exporter textfile collector, appliance ScrapeConfig, Cilium egress policy,
storage-appliance dashboard, zvol→PVC recording rules).

Design authority: `docs/design/node-metrics.private.md`, `docs/design/storage-observability.private.md`,
`docs/design/storage-appliance-checkpoint.private.md`.

Rounds so far: 0, 1, 2.

| ID | Location | First cycle | Last cycle | Status | Final severity | Vote summary | Resolution | Reopen policy |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F0-1 | `apps/prometheus/components/prometheus/index.ts` (`nodeExporterExtraArgs()` helper, ~:37-66; `extraHostVolumeMounts`); rendered `deploy/prometheus/app.k8s.yaml:8806-8808, 8876-8882` | 0 | 1 | settled | verified | Reopened in round 1 because the round-0 revision changed this exact area; 3/3 verified. ROUND-0 FIX CONFIRMED PRESENT AND CORRECT in the live text: `nodeExporterExtraArgs()` emits the chart's two filesystem-exclude flags ahead of `--collector.textfile.directory`, and two voters independently byte-compared both literals against the pinned chart's own `values.yaml:2735-2736` (kube-prometheus-stack 88.1.3 tarball) with Python string equality returning True for BOTH, no whitespace or escaping drift. One voter re-ran the CANONICAL render (`earthly +k8s-manifests`, SUCCESS) and confirms `deploy/prometheus/app.k8s.yaml:8806-8808` carries all three args in order. `extraHostVolumeMounts` was deliberately left untouched and is safe: subchart default is `[]` (`charts/prometheus-node-exporter/values.yaml:496`) so nothing is clobbered; it renders `hostPath {path: /var/lib/prometheus/node-exporter, type: DirectoryOrCreate}` with a readOnly mount, and the exporter (uid 65534, readOnlyRootFilesystem) can read what the agent writes at mode 0644 (`textfile.go:22`). | verified — no action; round-0 blocker F0-1 is fixed correctly and completely | Reopen only if `nodeExporterExtraArgs()`, node-exporter `extraArgs`/`extraHostVolumeMounts`, or the pinned chart version changes in a round >= 1 |
| F0-2 | `apps/prometheus/components/network-policy.ts:19-35` (construct renamed `prometheus-storage-appliance-egress` -> `prometheus-egress`); rendered `deploy/prometheus/app.k8s.yaml:13963-14006` | 0 | 1 | settled | verified | Reopened in round 1 because the round-0 revision changed this exact file; 3/3 verified. ROUND-0 FIX CONFIRMED PRESENT AND CORRECT: CCNWP `prometheus-prometheus-egress` now carries six egress rules — `toEntities` cluster / host / remote-node / kube-apiserver, a kube-dns `toEndpoints` rule on 53 UDP+TCP, and the original `toCIDR 10.10.9.250/32` on 9100/TCP — matching the in-repo reference `apps/longhorn/components/network-policy.ts:22-28`, and declares NO `ingress` key, so Prometheus' ingress is unaffected. The endpointSelector is the two-label Prometheus-server pair scoped to namespace prometheus; a live `kubectl` query with exactly that selector returns exactly one pod, so Grafana, Alertmanager, kube-state-metrics, the operator and the node-exporter DaemonSet are NOT swept into default-deny egress. No other repo policy grants the server egress this one had to preserve, no remote_write or other external target is configured, and 10.10.9.250 is not a cluster node so it genuinely needs the explicit toCIDR. The source construct now carries a comment naming the default-deny mechanism so the next editor cannot silently re-break it. | verified — no action; round-0 blocker F0-2 is fixed correctly | Reopen only if the prometheus egress policy, `EndpointNetworkPolicy`, or apps/prometheus' policy composition changes in a round >= 1 |
| F0-3 | `apps/prometheus/components/dashboards/storage-appliance.json` panel id 15, targets A and B (file lines 789, 796) | 0 | 1 | settled | verified | Reopened in round 1 because the round-0 revision changed these exact expressions; 3/3 verified. ROUND-0 FIX CONFIRMED CORRECT IN BOTH DIRECTIONS: both targets are now `absent(k2_zfs_last_snapshot_timestamp_seconds{job="storage-appliance",instance="k2-st-0e12",prefix="k2-hourly"\|"k2-daily"}) or on() vector(0)`. MEASUREMENT 1 (prometheus v0.313.2 promqltest, instant eval): series present -> one labelless sample valued 0; series absent -> one sample valued 1. MEASUREMENT 2 (live Prometheus via port-forward /api/v1/query): target A returns 1 in the current no-series state. The `or on() vector(0)` tail is what makes the healthy branch a labelless 0 rather than an empty vector, so `unit: bool`, min 0 / max 1, green(base)/red(1) thresholds and `textMode: name` with static legendFormat all remain meaningful, and the red state is now reachable in exactly the condition the panel description claims. Prefix literals match `k2-snapshot-{hourly,daily}.service`. NOTE the fix is panel-15-local: `absent(` occurs exactly twice in the whole file, both here — which is what F1-4 is about. | verified — no action; round-0 blocker F0-3 is dead | Reopen only if panel 15's expressions, the snapshot-cadence series/prefixes, or the design's absence-vs-zero guidance changes in a round >= 1 |
| F0-4 | `kairos/node-agent/internal/textfile/textfile.go:11-16`; `kairos/overlays/role/k8s/oci/etc/systemd/system/k2-metrics.service`, `k2-image-check.service` | 0 | 0 | open | defer | 3/3 defer. Facts confirmed (no `MkdirAll` anywhere in the textfile package, no `ExecStartPre`/`StateDirectory` on either k8s unit, no prometheus-node-exporter package in the k8s role), but the consumer's next action still succeeds: the DaemonSet's hostPath `type: DirectoryOrCreate` makes kubelet create `/var/lib/prometheus/node-exporter`, and both timers keep re-firing (metrics every minute, image-check hourly), so the writer self-heals. Named future cost: a failed unit in `systemctl --failed` after every boot on 7 nodes, up to an hour's gap in `k2-image.prom`, an undocumented ordering dependency of a systemd unit on a Kubernetes DaemonSet, and a node class with no node-exporter pod where it never heals. Note the design doc's claim that "the metrics writer creates the dir itself today" is false against the code. | defer — add `ExecStartPre=/usr/bin/install -d -m 0755 /var/lib/prometheus/node-exporter` (or `StateDirectory=`) to both k8s units, or `os.MkdirAll` in textfile.Write; correct the design doc's claim | Reopen if textfile.Write, either k8s unit, the k8s role package list, or the node-exporter hostPath changes in a round >= 0 |
| F0-5 | `kairos/node-agent/internal/metrics/metrics.go:138-152` (`Collector.Render` storage role gate) | 0 | 0 | open | defer | 1 blocking / 2 defer → defer (fewer than half at blocking-or-higher). The blocking voter's consequence ("cannot distinguish 'appliance healthy' from 'metadata.yaml unreadable or mismatched'") was outweighed by two voters who found the blind spot narrower than described: the read-error case still emits `k2_collector_success{collector="node_build"} 0`, which dashboard panel 23 renders, and metadata.yaml is baked into every image by `kairos/Dockerfile:237-253` with `role` only ever taking the literals from targets.yaml. Design §4.3's skip-rather-than-emit-zero instruction is scoped to non-storage nodes and is honoured there. Future cost: an unrecognized role value silently degrades a storage node to the k8s output shape with node_build still reporting success, and no test covers storage-role + unreadable metadata. | defer — validate `role` against a known set and fail loudly (or emit a distinguishing series) instead of degrading to the non-storage shape; add the missing test | Reopen if the role gate, collectNodeBuild's validation, or metadata.yaml's generation changes in a round >= 0 |
| F0-6 | `kairos/node-agent/internal/metrics/metrics.go:139` and `:166` (double `readBuildMetadata`) | 0 | 0 | open | advisory | 3/3 advisory. A ~13-line image-baked file read twice per minute; the disagreement window requires the file to change mid-collection, which only happens on an image swap. Nothing predictable is lost by ignoring it. | advisory — thread the already-read map into collectNodeBuild if touching this code anyway; worth revisiting only if the metadata source ever becomes dynamic | Reopen if the metadata source becomes dynamic or Render/collectNodeBuild is restructured in a round >= 0 |
| F0-7 | `kairos/node-agent/internal/metrics/metrics.go:225-253` (`renderExposition` label-count mismatch skip) | 0 | 0 | open | defer | 3/3 defer. Confirmed: the `continue` fires after the HELP/TYPE header is already written, with no error, no log, and no effect on `k2_collector_success`, and `TestRenderExpositionSkipsMismatchedLabels` codifies the drop as intended. No current descriptor mismatches, so nobody is stopped today. Named future cost: this is the same silence class as the single-label bug the rewrite existed to kill — the next collector added with a miscounted label list reproduces the original failure mode (family header, zero samples, success still 1). | defer — make the mismatch loud: debugf on the skip path and/or fail the group's collector_success | Reopen if renderExposition, the descriptor/sample types, or any new collector's label sets change in a round >= 0 |
| F0-8 | `apps/prometheus/components/dashboards/index.ts:307-330`; consumers of `k2_node_image_check_*` | 0 | 0 | open | defer | 1 blocking / 2 defer → defer. MEASUREMENT: `grep -rn "alert:" apps/` hits only vendored CRD schemas; the sole repo-authored PrometheusRule has two `record:` entries and zero `alert:`. AUTHORITY: `docs/design/node-metrics.private.md` §3 calls `k2_node_image_check_success` alerting "not optional" (check_success == 0, stale check_timestamp_seconds, absent()-guarded upgrade_available == 1). Deciding rationale: textbook deferred work — the operator can deploy, panel 30 renders check success/age/upgrade availability, and nothing they do next fails; the cost is future and predictable (a stale image or permanently failing hourly check is visible only to someone who opens the dashboard). Compounds F0-3: with no alerts anywhere, every failure signal in this change is dashboard-glance-only. | defer — add the three alert rules from design §3 alongside the recording rules (and note the repo has no author-written alerts at all today) | Reopen if any alert rule lands, or if the image-check metric names/semantics change in a round >= 0 |
| F0-9 | `apps/prometheus/components/dashboards/index.ts:24-43` (PrometheusRule inside `GrafanaDashboards`) | 0 | 0 | open | defer | 3/3 defer. Renders correctly (tsc clean, rule present in deploy/), so nobody is stopped. Named future cost: a later reader searching for recording rules under a components directory named `dashboards` will not find them, and the next rule gets added there by precedent. | defer — hoist the PrometheusRule out of GrafanaDashboards, alongside the ScrapeConfig in the prometheus component | Reopen if the dashboards component or the recording rules are relocated/extended in a round >= 0 |
| F0-10 | `apps/prometheus/components/prometheus/index.ts:129-133` (`storageApplianceTarget` static `role: storage` label) | 0 | 0 | open | advisory | 1 defer / 2 advisory → advisory. Mechanism confirmed (honorLabels unset ⇒ the exposed `role` becomes `exported_role` on appliance series) but no consequence found: both values are "storage", and panel 29's `max by (…, role, …)` enumerates its grouping labels so `exported_role` never reaches the table. Residual: a future disagreement between metadata.yaml's role and the static target label would be masked by the target label winning. | advisory — dropping the static `role` target label (k2_node_build_info already carries one) would be strictly cleaner | Reopen if honorLabels, the static target labels, or panel 29's grouping changes in a round >= 0 |
| F0-11 | `apps/prometheus/components/dashboards/storage-appliance.json` panels 29 and 30 ("node" column); node-exporter ServiceMonitor relabelings | 0 | 0 | open | defer | 3/3 defer. Verified against the rendered manifest (`deploy/prometheus/app.k8s.yaml:13137-13161`: no `relabelings:` block; subchart values.yaml:211 `relabelings: []`, hostNetwork true), so the 7 k8s nodes keep `instance` = nodeIP:9100 while the appliance is pinned to `k2-st-0e12`. Every row is present and correct, so nobody is stopped; the "node" column just mixes one name with seven IP:port strings and every future reader pays the translation cost. | defer — add a `prometheus.monitor.relabelings` entry mapping `__meta_kubernetes_pod_node_name` to `instance` | Reopen if the node-exporter ServiceMonitor relabelings, the appliance instance label, or panels 29/30 change in a round >= 0 |
| F0-12 | `apps/prometheus/components/dashboards/storage-appliance.json` panel 27 (`transformations.organize.excludeByName`) | 0 | 0 | open | defer | 3/3 defer. Label flow traced: the static `role="storage"` target label lands on the zvol families, `*` with group_left preserves all left-hand labels, and panel 27's excludeByName omits `role` while indexByName positions only namespace/PVC/Value #A/Value #B — so a stray constant "role" column appears at the end. (No `exported_role` here; that collision is confined to k2_node_build_info.) Purely cosmetic, table still usable. | defer — add `role` to excludeByName (or drop the static role target label per F0-10) | Reopen if panel 27's transformations, the recording rule's label set, or the static target labels change in a round >= 0 |
| F0-13 | `apps/prometheus/components/dashboards/storage-appliance.json` `templating.list[0]` (`zvol`); `dashboards/index.ts` `storageZvolPvcRecordingRule()` | 0 | 0 | open | defer | 2 defer / 1 advisory → defer. Confirmed: the variable enumerates every zvol from the raw family while panels 26/27 read the recorded PVC-joined families, which exist only for datasets matching `.*/(pvc-[^/]+)$` that join `kube_persistentvolumeclaim_info`. Panel 26's description already says "No data means no matching k2-iscsi PVC exists", so the operator is warned rather than misled, and on the real appliance every zvol comes from democratic-csi under `tank/csi/k2`. Named future cost: the zvol an operator most wants to inspect (a leak or non-CSI volume) is exactly the one the picker offers and the PVC panels cannot show. | defer — either scope the variable to the recorded series, or add a raw-family panel that covers orphan zvols | Reopen if the zvol variable query, the recording rule regex/join, or panels 26/27 change in a round >= 0 |
| F0-14 | `apps/prometheus/components/dashboards/storage-appliance.json` (21 inline `instance="k2-st-0e12"`); `components/prometheus/index.ts:132`; `components/network-policy.ts:23` | 0 | 0 | open | defer | 3/3 defer. Confirmed by measurement: 21 JSON-escaped `instance="k2-st-0e12"` occurrences plus `10.10.9.250` written twice (scrape target, egress CIDR) with no shared constant, while `apps/democratic-csi/constants.ts:1-7` already holds appliance addressing for the same physical box. Everything renders. Named future cost: the scrape target and the egress CIDR are two halves of one fact that can drift independently, and re-addressing or renaming the appliance means hand-editing 20+ dashboard expressions. | defer — hoist the LAN address into a shared appliance-constants module and introduce a dashboard template variable for the instance | Reopen if the appliance address/name changes, or if the constants module or dashboard variables are reworked in a round >= 0 |
| F0-15 | `kairos/node-agent/internal/imagecheck/imagecheck.go:23-24` (`defaultTokenURL`, `defaultRegistryURL`) | 0 | 0 | open | defer | 3/3 defer. Confirmed: `wyvernzora/k2-kairos` is embedded twice in imagecheck with no cross-reference to `kairos/versions.env` REGISTRY_IMAGE or `tools/internal/step/upgrade/resolve.go` DefaultRegistryRepo — even though this same change added exactly that kind of two-way "keep aligned" comment for slotTag/tagSegments (imagecheck.go:153-154, plan.go:440-441). Named future cost, asymmetric: a registry move leaves the node agent resolving the old repo and reporting `upgrade_available` against the wrong image while `check_success` stays 1. | defer — add the two-way cross-reference comment at minimum; the real fix is a `registry:` field in the Dockerfile metadata block the node can read | Reopen if the registry repo moves, or if imagecheck's URL constants / the Dockerfile metadata fields change in a round >= 0 |
| F0-16 | `kairos/node-agent/cmd/k2-node-agent/main.go:39-45`; `internal/imagecheck/imagecheck.go:21` (`DefaultTextfileDir`) | 0 | 0 | open | advisory | 1 defer / 2 advisory → advisory. Sharing `K2_NODE_AGENT_METRICS_TEXTFILE_DIR` across both subcommands is arguably correct — both must write into the same node_exporter textfile directory, and wanting them split is hypothetical. The 4x path literal is contained within one Go module and would fail loudly (empty textfile dir) rather than drift silently; `imagecheck.DefaultTextfileDir` being exported with no external caller is minor dead API. | advisory — optionally collapse the path literal to one exported constant and unexport the unused one | Reopen if the two subcommands need distinct directories, or if the textfile path/env plumbing changes in a round >= 0 |
| F0-17 | `kairos/overlays/role/{k8s,storage}/oci/etc/systemd/system/k2-image-check.{service,timer}` | 0 | 0 | open | defer | 3/3 defer. Confirmed: no `Wants=`/`After=network-online.target` on a unit that makes an outbound HTTPS call, while the timer is `OnCalendar=hourly` + `Persistent=true` (RandomizedDelaySec=10min staggers but does not gate on connectivity). The consumer's next action still succeeds: a network-less run writes `k2_node_image_check_success 0` with a fresh timestamp — the designed distinguishable-failure shape — and the next hourly run recovers. Named future cost: a failed unit in `systemctl --failed` after most boots on 8 nodes, training the operator to ignore that list. Convention is not a defence here — no prior overlay unit made a timer-driven network call. | defer — add `Wants=network-online.target` / `After=network-online.target` to both roles' k2-image-check.service | Reopen if either k2-image-check unit/timer, or the overlay's network-ordering convention, changes in a round >= 0 |
| F0-18 | `kairos/overlays/role/k8s/oci/etc/systemd/system/k2-metrics.service` and `.timer` (Description text) | 0 | 0 | open | defer | 3/3 defer. Confirmed byte-for-byte: both k8s units say "K2 storage appliance metrics …" while installed on 3 rpi4cb control-plane nodes and 4 qemu workers, where role gating means they emit only k2_node_build_info plus one collector_success series. Zero functional effect (the overlay inspect assertions do not check Description). Named future cost: exactly the "prose that will mislead a later reader" case — an operator running `systemctl status`/`list-timers` on a CP Pi is told the box is a storage appliance. Two string edits. | defer — reword both k8s unit Descriptions to name node metrics rather than the storage appliance | Reopen if either k8s unit file or the overlay Description assertions change in a round >= 0 |
| F0-19 | `kairos/overlays/role/k8s/oci/system/oem/06-iscsi.yaml:33-38` (timer-enable stage in the iSCSI document) | 0 | 0 | open | defer | 1 advisory / 2 defer → defer. Confirmed: the document is `name: "K2 iSCSI initiator"` with an IQN-only header comment, and the "Enable K2 node metrics timers" stage was appended to it, whereas the storage role uses a purpose-named `10-storage-services.yaml`. It works and the overlay assertions correctly target `/stages/initramfs/1/`. Named future cost: discoverability (nobody looks for metrics timers in the iSCSI cloud-config) plus coupling — a future iSCSI stage reorder silently invalidates the index assertions, and the file's name now lies about its contents. | defer — move the stage into a sibling `07-node-metrics.yaml` with its own inspect assertions | Reopen if 06-iscsi.yaml's stage list, the k8s overlay inspect assertions, or the k8s cloud-config layout changes in a round >= 0 |
| F0-20 | `kairos/node-agent/internal/imagecheck/imagecheck.go:28-29, 196-233, 247-276` (marker-absent fallback: 307 to a second host, 5 sequential requests in a 30s budget) | 0 | 0 | open | defer | 2 defer / 1 advisory → defer. Both sub-facts verified in source (10s per-request / 30s overall; token → HEAD manifest → GET index → GET image manifest → GET config blob; ghcr blob GETs 307 to pkg-containers.githubusercontent.com, measured live 2026-08-10). No consumer is stopped: these are host-level systemd services on unrestricted LAN egress, and any failure surfaces loudly as `k2_node_image_check_success 0`. Named future cost: nodes flashed from a raw artifact (no `/usr/local/.state/k2-image-digest`) are permanently on the five-request path — steady state, not an edge case — and the second host is a documentation gap in the design's "verify ghcr egress from every node class" constraint that will bite whoever adds host firewalling. | defer — document the pkg-containers.githubusercontent.com dependency in the design's egress constraint; consider raising the overall budget or short-circuiting the fallback | Reopen if the timeouts, the fallback request sequence, host egress filtering, or the digest-marker provisioning changes in a round >= 0 |
| F0-21 | `kairos/node-agent/internal/metrics/metrics.go:225-253` (renderExposition); `metrics_test.go:210-215` | 0 | 0 | settled | verified | 3/3 verified. Checked in live text: the renderer iterates `d.labels` emitting `%s=%q` per label with a `{…}` wrapper only when the descriptor has labels, so multi-label, single-label and unlabeled families all render intact — the original "desc carries ONE label name, multi-label samples render with ALL labels dropped" defect is structurally gone. The design's demanded regression assertion exists on the RENDERED TEXT (the literal 11-label `k2_node_build_info` line), not the sample struct. `(cd kairos/node-agent && gofmt -l . && go vet ./... && go test ./...)` exits 0. | verified — no action | Reopen only if renderExposition or its regression tests are revised in a round >= 0 |
| F0-22 | `kairos/node-agent/internal/metrics/metrics.go:102-128`; `internal/imagecheck/imagecheck.go:286-304` | 0 | 2 | settled | verified | 3/3 verified. `k2_node_build_info` descriptor carries exactly the 11 documented labels, is emitted with value 1, and the renderer writes `# TYPE <name> gauge`; `k2_node_image_info` likewise. Standard `_info` convention. AUTHORITY `docs/design/node-metrics.private.md:127-142` explicitly accepts source_commit's per-build series churn at this fleet size (~8 nodes), so the cardinality is an operator decision already taken. ROUND 2: proactively reopened (no reviewer raised it) because round 1 edited metrics.go:119, which sits INSIDE this finding's recorded range :102-128; 3/3 verified — the settled verification holds. `k2_node_build_info` still declares exactly the 11 documented labels in the documented order (target, flavor, flavor_version, kairos_version, kairos_agent_version, kubernetes_distro, kubernetes_version, role, arch, hardware, source_commit); every other descriptor in the block is structurally unchanged and no descriptor gained or lost a label. The only line differing from the round-0-verified block is :119's lioSessions HELP string (see F1-10). Renderer behaviour is settled separately as F0-21 and was not touched. | verified — no action | Reopen only if the info-metric label sets, types, or the fleet-size cardinality assumption change in a round >= 2 |
| F0-23 | `kairos/node-agent/internal/imagecheck/imagecheck.go:72-98, 117-151, 286-303`; `k2-image-check.timer` in both roles | 0 | 0 | settled | verified | 3/3 verified. Run writes the textfile from `render(result, …)` BEFORE inspecting checkErr, so `k2_node_image_check_success` and `_timestamp_seconds` are emitted on every completed attempt and only upgrade-availability/image-info are gated on success — a registry outage cannot masquerade as "no upgrade". Marker comparison with source-commit fallback at :117-151. Tests cover digest match/mismatch, OCI-index fallback, registry failure, token failure and both slot-tag cases. Both roles' timers: `OnCalendar=hourly`, `Persistent=true`, `RandomizedDelaySec=10min`. Caveat carried as F0-4: this failure path is unreachable when the textfile directory itself is missing. | verified — no action | Reopen only if imagecheck.Run's write/error ordering, the rendered families, or either timer changes in a round >= 0 |
| F0-24 | `kairos/node-agent/internal/imagecheck/imagecheck.go:154-165` (slotTag); `tools/internal/image/plan/plan.go:442-454` (tagSegments) | 0 | 0 | settled | verified | 3/3 verified. slotTag composes `<flavor-family>-<arch>-<hardware>-<role>` from metadata.yaml, identical to tagSegments — including the k8s case where only `DescriptiveTag` appends the k3s segment (the floating slot tag is deliberately version-free). The requested two-way cross-reference comments are present in both files. MEASUREMENT (live, anonymous pull token, 2026-08-10): all five current slot tags resolve 200 on ghcr. The malformed-flavor edge case (leading "-") is unreachable given `kairos/targets.yaml`, and erroring there is the safer behaviour. | verified — no action | Reopen only if slotTag, tagSegments/DescriptiveTag, or the target matrix changes in a round >= 0 |
| F0-25 | `kairos/node-agent/internal/imagecheck/imagecheck.go:117-129, :189` vs `tools/internal/step/upgrade/upgrade_active.go:22-25` and `tools/internal/client/oci/discover.go:241-246` | 0 | 0 | settled | verified | 3/3 verified, and flagged by voters as the most dangerous possible silent defect in the phase. The marker is `plan.Target.Digest` from `crane.Digest(ref)` with no options; AUTHORITY go-containerregistry v0.21.9 `pkg/crane/digest.go:20-51` + `options.go:51-59` — with a nil Platform it takes the `Head(ref)` path and returns the tag's own (index) manifest digest, which is exactly what the registry reports in `Docker-Content-Digest` and what imagecheck reads. So `appliedDigest != registryDigest` is index-vs-index on every architecture, not index-vs-per-arch. MEASUREMENT (live 2026-08-10): HEAD `manifests/ubuntu-amd64-qemu-storage` → oci image index, digest sha256:7474dd79…, matching the design's recorded appliance digest. | verified — no action | Reopen only if the marker writer, crane invocation, or the digest comparison changes in a round >= 0 |
| F0-26 | `kairos/node-agent/internal/imagecheck/imagecheck.go:33-34, 196-233`; `imagecheck_test.go:179-186` | 0 | 0 | settled | verified | 3/3 verified. `manifestAccept` lists all three media types the design's GOTCHA requires and is sent on both the HEAD and GET manifest requests; the test server returns 406 when any one is missing, so the header is asserted rather than assumed. The `len(matches) != 1` linux/<arch> filter matches the live index shape (one linux manifest + one unknown/unknown attestation manifest), and the config blob's `org.opencontainers.image.revision` carries the K2 source commit (ea9b80de…), not the Ubuntu base's. | verified — no action | Reopen only if manifestAccept, the index-traversal filter, or the revision-label source changes in a round >= 0 |
| F0-27 | `kairos/overlays/role/storage/overlay.yaml:40-52` + `10-storage-services.yaml:43-58`; `kairos/overlays/role/k8s/overlay.yaml:146-154` + `06-iscsi.yaml` | 0 | 0 | settled | verified | 3/3 verified, two voters re-deriving the index sets from the files rather than trusting the report. Storage enable list grew 11→12 by inserting `k2-image-check.timer` at index 8; all five new `op: test` assertions at indices 7-11 match and the pre-existing assertions at 0/1/5 are unshifted. k8s assertions target `/stages/initramfs/1/`, which is the new "Enable K2 node metrics timers" stage with enable/0=k2-metrics.timer, enable/1=k2-image-check.timer, and the four new `contains` blocks match the unit files byte-for-byte. `kairos/Dockerfile:296-298` tar-copies `oci/` to `/`, so the units land where the inspect paths expect. Index-assertion drift was the likeliest way this phase could have broken the image build; it did not. | verified — no action | Reopen only if either overlay's enable lists or inspect assertions change in a round >= 0 |
| F0-28 | `apps/prometheus/components/prometheus/index.ts`; `apps/prometheus/crds/crds.k8s.yaml`; both role overlays | 0 | 0 | settled | verified | 3/3 verified. `extraArgs` and `extraHostVolumeMounts` (incl. the `type:` sub-key) are the correct prometheus-node-exporter 88.1.3 value keys, consumed by templates/daemonset.yaml, and the textfile hostPath volume/mount render as intended. `scrapeConfigSelector`/`scrapeConfigNamespaceSelector` reach the Prometheus CR as `{}`/`{}`, the scrapeconfigs CRD is already vendored, the operator ClusterRole grants it, and `spec.jobName` sets the `job` label — which is what makes `job="storage-appliance"` valid in the recording rule and 20 panel selectors. Cilium egress hits the exact address/port; both roles' timers are enabled. SCOPE BOUNDARY recorded by two voters: this verifies the extraArgs KEY is right; F0-1 is about the VALUE replacing the chart defaults. Both hold simultaneously. | verified — no action | Reopen only if the helm value keys, ScrapeConfig plumbing, CRD/RBAC bundle, or chart version changes in a round >= 0 |
| F0-29 | `apps/prometheus/components/dashboards/storage-appliance.json` (whole file); `apps/prometheus/components/dashboards/index.ts:318-331` (`storageZvolPvcRecordingRule`) | 0 | 2 | settled | verified | Proactively reopened in round 1 under its own reopen policy (the round-0 panel-15 edit changed the dashboard JSON); 3/3 verified. RE-MEASURED after that edit: `json.load()` succeeds, the file still carries exactly 30 panels with unique ids 1..30 (no gaps, no duplicates), the zvol template variable and row panels are intact, and the 21 `instance="k2-st-0e12"` occurrences are unchanged. The recording rule is untouched by round 0 and renders identically in the canonical manifest (`deploy/prometheus/app.k8s.yaml:7038,7040`); its join semantics were re-measured under promqltest — one series per matching zvol preserving job/instance/role/volume and adding namespace/persistentvolumeclaim/volumename, dropping the non-PVC dataset `tank/csi/k2` on the regex and a Pending PVC on the `volumename!=""` guard, empty result with zero zvols (what panels 26/27 rely on), no KSM label leakage. SCOPE BOUNDARY, sharpened this round: this certifies structure and HAPPY-PATH join semantics ONLY — it does NOT cover the duplicate-volumename abort (F1-9) nor the absence-handling gaps (F1-2, F1-4, F1-11), all of which live inside syntactically valid panels. ROUND 2: reopened again under its own policy (round 1 edited panel 11 inside the dashboard JSON, net -3 lines); 3/3 verified. RE-MEASURED after that edit by two voters and the collector independently: `json.load()` succeeds, `len(ids), len(set(ids)), ids==list(range(1,31))` prints `30 30 True`, and a `jq -e` equivalent exits 0; `git diff --check` exits 0 (no trailing-whitespace or conflict damage). Structure intact. SCOPE unchanged from round 1: certifies structure and happy-path join semantics only, not absence handling (F1-2/F1-4/F2-9) or description accuracy (F2-11). | verified — no action | Reopen only if the dashboard JSON, the recording rule, or the KSM/democratic-csi label sources change in a round >= 2 |
| F0-30 | `kairos/node-agent`; `tools`; `apps/prometheus`; required verification command set | 0 | 3 | settled | verified | Proactively reopened in round 1 under its own reopen policy ("to record the earthly results once run"); 1 defer / 2 verified -> verified (>= half verified). ROUND-0 CAVEAT NOW FULLY DISCHARGED BY EXECUTION, not by plan: two voters independently RAN `earthly +k8s-manifests` against this working tree — `🌍 Earthly Build ✅ SUCCESS` at 2026-08-10 21:29, all app rows ok (prometheus ok 7.868s), only the three pre-existing warnings (blocky CRD probe, cilium allow-crd-empty-render, longhorn CRD strip). It re-exported deploy/ at the same 873366 bytes and the same line numbering as the 20:49 local render every reviewer cited, and the canonical tree carries all three round-0 fixes (:8806-8808 the three node-exporter args, `prometheus-prometheus-egress` at :13963-14006, two `absent(k2_zfs_last_snapshot...)` occurrences). Combined with the coordinator's post-revision result (both Go module suites pass, `earthly +lint` green), the FULL required verification set is now recorded as executed and passing against this tree. The dissenting defer vote objected only that no reviewer had confirmed the canonical render agrees with the local one — which the two executions answer. deploy/ remains gitignored (`.gitignore:76`) by design, which is not a gap. ROUND 2: reopened again (round 1 revised one Go file and the dashboard JSON); 3/3 verified. FRESH RESULT: `(cd kairos/node-agent && gofmt -l . && go vet ./... && go test ./...)` re-run by the collector and independently by two voters — gofmt silent, vet silent, ok for cmd/k2-node-agent, internal/health, internal/imagecheck, internal/metrics, internal/snapshot, internal/storage, with buildmetadata/runner/textfile `[no test files]` (settled F1-7). `(cd tools && go test ./...)` exit 0. NAMED CAVEAT, recorded rather than hidden: neither `earthly +lint` nor `earthly +k8s-manifests` was re-run against the round-1 text by any reviewer this round (one sandbox had no Docker/Podman and could not create a Go work dir); the coordinator's post-round-1 re-verification remains the most recent evidence for those two. Voters judged the residual risk small because round 1 touched exactly one Go HELP string (covered by the green gofmt/vet/test run) and one JSON file (structural validity independently re-measured under F0-29). ROUND 3: proactively reopened (round 2 changed kairos/node-agent saveConfigInSync + panel 12 and tools' marker write); 3/3 verified. Re-executed this round: `(cd kairos/node-agent && gofmt -l . && go vet ./... && go test ./...)` and `(cd tools && gofmt -l . && go vet ./... && go test ./...)` both clean (buildmetadata/runner/textfile and workflow/upgrade report [no test files], the latter booked under F3-2). One voter also re-ran `earthly +lint` and `earthly +k8s-manifests` successfully against this tree, discharging the carried caveat for round 2 text; the other two verified the canonical render directly instead (deploy/prometheus/app.k8s.yaml, 873757 bytes, contains the round-2 panel strings 'LIO target count matches saveconfig' and 'LIO node ACLs' exactly once each, ConfigMap k2-grafana-dashboard-k2-storage-appliance :4120, PrometheusRule storage-zvol-pvc :7029, ScrapeConfig jobName storage-appliance :7041-7055 with static target 10.10.9.250:9100 and labels instance=k2-st-0e12/role=storage, node-exporter arg --collector.textfile.directory=/var/lib/prometheus/node-exporter :8805 with readOnly hostPath type DirectoryOrCreate). ORDERING RE-CONFIRMED: none of the four cluster-side objects references a series or file only the rebuilt node image provides, so applying the cluster half first yields empty panels, not manifest or rule-load failures. Explicitly NOT contradicted by F3-1/F3-8, neither of which is a compile-, vet- or render-level failure. | verified — no action; re-run `earthly +lint` and `earthly +k8s-manifests` after the F3-1/F3-8 fixes land | Reopen if the verification command set changes, or if a later revision lands without re-running it, in a round >= 2 |
| F0-31 | `kairos/node-agent/internal/textfile/textfile.go:11-31` | 0 | 0 | settled | verified | 3/3 verified. The temp name `filepath.Base(path)+".tmp-*"` does not match node_exporter's `*.prom` glob, so an in-progress write is invisible rather than half-parsed; the sequence is CreateTemp → WriteString → Chmod(0o644) → Close → same-directory `os.Rename`, with `defer os.Remove(tmp.Name())` on every error path. A concurrent reader sees either the old complete file or the new complete file. Correct pattern, and it survives a 60-second writer against an independently-scheduled collector. | verified — no action | Reopen only if textfile.Write's write/rename sequence or the temp-name pattern changes in a round >= 0 |
| F1-1 | `apps/prometheus/components/prometheus/index.ts:131` (`storageApplianceScrapeConfig`, `jobName: "storage-appliance"`); rendered `deploy/prometheus/app.k8s.yaml:7052`, bundled rules `:12302-12423`, TargetDown `:9880-9886` | 1 | 1 | open | defer | 1 blocking / 2 defer -> defer (fewer than half at blocking-or-higher). FACT CONFIRMED against the canonical render by all three voters: `jobName: storage-appliance` at :7052, while every bundled node-exporter alert pins the literal `job="node-exporter"` (22 distinct Node* alert names across 27 blocks; ~72-87 occurrences of the literal file-wide), including `:12421 node_textfile_scrape_error{job="node-exporter"} == 1` — the only bundled detector for a malformed textfile, i.e. the exact mechanism this change introduces on that host. The appliance is therefore outside NodeFilesystemAlmostOutOfSpace, NodeClockNotSynchronising, NodeRAIDDegraded, NodeSystemdServiceFailed, the stack's node-exporter recording rules and its bundled Node Exporter dashboards; only the job-agnostic TargetDown (`:9885`, no job filter) reaches it. The blocking voter's consequence: "An on-call operator cannot rely on the bundled NodeTextFileCollectorScrapeError to detect a malformed k2.prom/k2-image.prom on the appliance ... and would instead have zero automated detection of a broken textfile collector on the appliance." The two defer voters could not complete that sentence: the operator applies, the appliance is scraped, the dashboard renders, and nothing regresses — before this change the appliance was not scraped AT ALL, so this is strictly additive coverage, and TargetDown still fires on unreachability at 10m. NAMED FUTURE COST: the box holding all the data permanently sits outside every Node* alert, with dashboard-glance as the only signal. | defer — book alongside F0-8 (no author-written alerts at all). NOTE renaming the job is NOT a free swap: the two recording rules and the dashboard's 21 `job="storage-appliance"` selectors would have to move with it, so the cheaper route is a second job label on the ScrapeConfig or appliance-scoped copies of the handful of Node* rules that matter | Reopen if the ScrapeConfig `jobName`, the dashboard's job selectors, or the alert-rule set covering the appliance changes in a round >= 1 |
| F1-2 | `kairos/node-agent/internal/metrics/metrics.go:269-303` (`collectZFSPools`), `:306-337` (`collectZFSKeyStatus`); `storage-appliance.json` panels 2, 3, 6, 7, 16, 23 | 1 | 1 | open | defer | 3/3 defer. FACT CONFIRMED by reading the source: both collectors set `result := groupResult{success: true}` before the per-line loop and EVERY `result.success = false` assignment (`:279`, `:288`, and the keystatus equivalents at `:310-312`) sits INSIDE that loop, so a zero-exit command with no output lines yields success with zero samples. No voter could complete blocking's sentence, for two independent reasons. (1) REACHABILITY UNMEASURED: nobody has a ZFS host to confirm `zpool list -Hp` actually exits 0 with genuinely empty stdout on an unimported pool (the typical zero-pools output is a non-empty line that would fail parsing and correctly flip success=false). (2) THE OPERATOR IS NOT LEFT REASSURED: panels 2 (Pool ONLINE) and 3 (Pool key available) use a RED base threshold step, so their No-data placeholder renders red exactly in this state — panel 23 showing zfs_pools=1 beside two red pool tiles is contradictory, not silent. NAMED FUTURE COST: panel 23's own description asserts "a failed collector stays visible even when the metrics it owns disappear", and in the pool-gone case it affirmatively reports healthy, so the next reader trusts that row more than it deserves. | defer — one line per collector: track whether any sample was produced and clear `success` when a group returns empty (same fix shape as F1-11); optionally measure `zpool list -Hp` against an unimported pool to settle reachability | Reopen if `collectZFSPools`/`collectZFSKeyStatus`'s success accounting, or panels 2/3/6/7/16/23's absence handling, changes in a round >= 1 |
| F1-3 | `storage-appliance.json` panel id 5 ("Storage health age"); `kairos/node-agent/internal/metrics/metrics.go:485-496` (`collectStorageHealth`) | 1 | 1 | open | defer | 1 advisory / 2 defer -> defer. FACT CONFIRMED: the `os.IsNotExist` branch returns `success: true` with `{storageHealthy 0, storageLastRun 0}` rather than omitting the timestamp, and panel 5 is `time() - k2_storage_health_last_run_timestamp_seconds` with unit `s` and steps green(null)/yellow(5400)/red(7200), so a never-written status file displays time()-0 ~= 1.79e9 s, rendered as roughly 57 years. Not blocking by unanimous agreement: the alarm DIRECTION is correct — the tile is red, far past the 7200 s step, with panel 4 (k2_storage_healthy, red base) red beside it — so the operator is warned rather than reassured and no decision changes. NAMED FUTURE COST: `k2-storage-health.timer` is OnBootSec=2min, so every appliance boot reproduces the window in which the dashboard shows a nonsense age, and the operator has to learn that "57 years" is the encoding for "never ran". | defer — omit the `storageLastRun` sample on the file-missing branch and let the absence treatment F1-4 asks for cover it, rather than emitting a sentinel 0 that arithmetic amplifies; or clamp the panel expression | Reopen if `collectStorageHealth`'s file-missing branch or panel 5's expression/thresholds changes in a round >= 1 |
| F1-4 | `storage-appliance.json` panel id 5 ("Storage health age") and panel id 20 ("Media errors") — `fieldConfig.defaults.thresholds` base step and absent `noValue`; contrast panel id 15 | 1 | 1 | open | defer | 1 blocking / 2 defer -> defer (fewer than half at blocking-or-higher). FACT CONFIRMED from three independent dumps of `fieldConfig.defaults`: panel 5 steps are green(base)/yellow(5400)/red(7200) and panel 20 green(base)/red(1), both with `noValue` unset, so Grafana paints the no-data placeholder with the GREEN base step — series absence is coloured identically to health on a staleness detector and a media-error detector. Panel 15 is the only panel in the file that converts absence into an explicit 0/1 (`absent(` occurs exactly twice, both there). The blocking voter's consequence: "A storage operator cannot trust a green panel 20 to mean zero media errors ... and would instead take no corrective action while a real, worsening media-error condition or a total collector outage goes undetected." The two defer voters could not complete it: in the scenarios that actually produce total absence, the operator is NOT looking at an all-green dashboard — panels 2/3/4/12/23 use a red base step and go red simultaneously, and a scrape failure is independently covered by TargetDown; the compound root cause (the `metrics.go:138-141` role gate skipping storage groups on an unreadable metadata.yaml) is itself the already-deferred F0-5. NAMED FUTURE COST: two health tiles structurally incapable of reporting "I have no data", in a dashboard whose stated design principle (panel 23's description) is that failure must stay visible — and the next person adding a panel copies whichever neighbour they land on. | defer — mechanical and per-panel: set `noValue` plus a red base step on panels 5 and 20, mirroring panels 2/3/4, or apply panel 15's `absent()` treatment | Reopen if panels 5 or 20's thresholds/noValue change, if the dashboard's absence-handling convention changes, or if F0-5's role-gate degradation is fixed, in a round >= 1 |
| F1-5 | `kairos/overlays/role/k8s/oci/etc/systemd/system/k2-metrics.timer`; `kairos/node-agent/internal/metrics/metrics.go:133-163` (`Render`), `:165-186` (`collectNodeBuild`) | 1 | 1 | open | defer | 1 advisory / 2 defer -> defer. FACT CONFIRMED: the k8s-role timer is `OnBootSec=1min` / `OnUnitActiveSec=1min`, and `Render` registers ONLY the `node_build` group unless `metadata["role"] == "storage"`. That group's two inputs — the image-baked `/usr/share/k2/image-build/metadata.yaml` (`kairos/Dockerfile:237-253`) and a fork of `kairos-agent --version` — are both invariant for the life of the booted image, so all 7 k8s nodes (3 of them rpi4cb) re-read the file, re-exec kairos-agent and atomically rewrite a byte-identical textfile ~1440 times per day to reproduce a constant. Nobody is stopped and the emitted output is correct, so not blocking. The advisory voter argued nothing predictable is lost (a file read plus one short-lived fork, byte-identical output, rename to /var/lib not SD-card-critical); the two defer voters booked it as a real predictable cost — ~10k subprocess execs and ~10k atomic rewrites per day fleet-wide, three of them on rpi4cb flash — noting the design doc justifies the 1-minute cadence ONLY by the storage collectors and moved the registry check to hourly for exactly this reason, while saying nothing about the k8s-role timer. Cheaper to change now than after the image ships. | defer — give the k8s-role timer a long `OnUnitActiveSec` or make it a boot-once oneshot, since its only collector is boot-invariant; pair with settled F0-18 (both k8s units are mistitled "K2 storage appliance metrics"), which touches the same two files | Reopen if the k8s-role `k2-metrics` timer/service cadence, or the set of collector groups `Render` registers for the k8s role, changes in a round >= 1 |
| F1-6 | `kairos/node-agent/internal/metrics/metrics.go:213-221` (`splitFlavor`); `tools/internal/image/plan/plan.go:446-454` (`flavorFamily`); `kairos/node-agent/internal/imagecheck/imagecheck.go:156-165` (`slotTag`, inline `strings.Cut`) | 1 | 1 | open | defer | 3/3 defer. Distinct from settled F0-24, which verified the slotTag<->tagSegments pair only. FACT CONFIRMED by reading all three sites: `metrics.go:213-214` carries a ONE-WAY comment naming plan.go; `plan.go:446-447`'s `flavorFamily` doc comment names no counterpart (git diff shows its only change this round is the tagSegments reciprocal two lines above at :440-441); and `imagecheck.go:157` does the split inline as a bare `strings.Cut(metadata["flavor"], "-")` with no comment at all. So the design's "a comment in each pointing at the other" is satisfied for slotTag/tagSegments and one-way for the flavor split, with a silent third copy. All three implementations agree today and plan_test.go:77,:102 pins the behaviour, so nothing built or run differs — not blocking. NAMED FUTURE COST, exactly the drift the design's cross-reference request existed to pre-empt: whoever edits `flavorFamily` is standing in the one copy with no back-pointer, changes tag construction in tools without touching the two node-agent copies that reconstruct the same tag on-node, and the mismatch surfaces as nodes querying a tag that does not exist. | defer — add the missing back-pointer on `flavorFamily`, and have `imagecheck.slotTag` call `splitFlavor` instead of re-cutting (same module, so no new shared module is needed) | Reopen if `splitFlavor`, `flavorFamily`, `slotTag`'s inline split, or the design's cross-reference requirement changes in a round >= 1 |
| F1-7 | `kairos/node-agent/internal/buildmetadata/metadata.go`; `kairos/node-agent/internal/textfile/textfile.go` (no `_test.go` in either new package) | 1 | 1 | open | defer | 3/3 defer, and named by one voter "the highest-value missing test in the change". FACT CONFIRMED: each directory contains exactly one .go file and `go test ./...` reports `[no test files]` for both. This is the severity model's own example of defer — coverage not yet written, consumer's next action unaffected, both suites green, and both packages exercised indirectly through metrics_test.go and imagecheck_test.go fixtures (textfile.Write's atomic-rename sequence was verified by inspection as settled F0-31). NAMED FUTURE COST is specific rather than generic: `buildmetadata.Read` is the single parser that `collectNodeBuild`, `Render`'s storage role gate and `imagecheck.check()` all depend on, and its consequential behaviours — lines without a colon silently skipped, key taken from the FIRST colon so colon-bearing values survive, and a file parsing to an ENTIRELY EMPTY map returned with a nil error — are precisely the input behind settled F0-5's silent storage->k8s degradation, so a regression there would be caught by nothing. | defer — a ~30-line table test over `buildmetadata.Read` pinning the three behaviours (no-colon skip, first-colon split, empty-map-nil-error), plus a direct test of `textfile.Write`'s rename sequence | Reopen if either package gains or changes behaviour, or if tests are added, in a round >= 1 |
| F1-8 | `kairos/overlays/role/{k8s,storage}/oci/etc/systemd/system/k2-image-check.service`; live `cilium-config`; `kairos/node-agent/internal/imagecheck/imagecheck.go:23-34` | 1 | 1 | open | verified | 3/3 verified — affirmative readiness result, not a defect. TWO THINGS CHECKED. (1) The image-check units are HOST systemd oneshots, not pods, so the new Cilium pod-egress policy is correctly irrelevant to them; the one cluster-side mechanism that could have silently blocked host egress was probed and is off — `kubectl -n cilium get cm cilium-config -o jsonpath='{.data.enable-host-firewall}'` returns `false` (re-measured independently by a second voter) and `policy-cidr-match-mode` is empty, so no CiliumClusterwideNetworkPolicy governs host egress to ghcr.io from the 7 k8s nodes or the appliance. (2) The registry path was exercised LIVE with the code's exact constants — token URL with no `service=` parameter returns a usable token, and HEAD with the exact three-type `manifestAccept` header returns 200 with a `docker-content-digest` for ALL FIVE enabled slot tags (ubuntu-amd64-qemu-storage, ubuntu-arm64-rpi4cb-k8s, ubuntu-amd64-qemu-k8s, ubuntu-arm64-qemu-k8s, ubuntu-arm64-qemu-storage), which also re-confirms settled F0-24's tag construction. TWO BOUNDARIES CARRIED, neither a defect here: the CP Pis' physical route to ghcr remains the design's own ASSUMPTION-untested, and the pkg-containers.githubusercontent.com fallback host stays booked under settled F0-20. | verified — no action | Reopen if `enable-host-firewall` is enabled cluster-wide, if the image-check's registry constants/Accept header change, or if the units move into pods, in a round >= 1 |
| F1-9 | `apps/prometheus/components/dashboards/index.ts:324-330` (`storageZvolPvcRecordingRule`), instantiated twice at :318-319; rendered `deploy/prometheus/app.k8s.yaml:7038, 7040` | 1 | 1 | open | defer | 1 blocking / 2 defer -> defer (fewer than half at blocking-or-higher). DEFECT IS REAL and confirmed verbatim in both source and canonical render: the join matches `on (volumename)` while the right-hand side is aggregated `max by (volumename, namespace, persistentvolumeclaim)`, so the two extra grouping labels are not in the match key and the aggregation does NOT guarantee one series per match group. REPRODUCED (promqltest v0.313.2): two `kube_persistentvolumeclaim_info` series sharing a volumename but differing in namespace/persistentvolumeclaim produce `ERROR: found duplicate series for the match group {volumename="pvc-aaa"} ... many-to-many matching not allowed` and ZERO output series — including for an unrelated healthy PVC pair in the same evaluation. Both rules share the expression, so the blast radius is both `k2_zfs_volume_pvc_size_bytes` and `k2_zfs_volume_pvc_used_bytes` for EVERY PVC. The blocking voter's consequence: "A cluster operator cannot use panels 26/27 to monitor storage across the fleet ... and would instead see 'No zvol PVCs' across the entire cluster while real capacity data for all volumes silently stops being recorded." The two defer voters could not complete it today: the rules evaluate, panels 26/27 populate, the operator deploys and reads identically, and the trigger needs an unusual PVC (explicit `spec.volumeName` onto an already-bound PV, still reported by KSM while Pending) whose reachability nobody confirmed on the live cluster; one voter also notes the failure is LOUD rather than silent — a rule evaluation error increments `prometheus_rule_evaluation_failures_total`, which the stack's bundled PrometheusRuleFailures alert watches (`deploy/prometheus/app.k8s.yaml:12961`). NAMED FUTURE COST: a rare-but-real operator action takes out both zvol-PVC families fleet-wide, and the resulting "No zvol PVCs" state is indistinguishable from legitimately having none. SHARPENS the scope boundary on verified F0-29, which covered the happy path only. | defer — dedup the right-hand side to one series per volumename, e.g. `topk by (volumename) (1, kube_persistentvolumeclaim_info{volumename!=""})`. NOTE the obvious fix is WRONG: shrinking to `max by (volumename)` drops the labels `group_left` needs. Fix in the same pass as F0-13 | Reopen if `storageZvolPvcRecordingRule`'s join, the KSM label source, or panels 26/27 change in a round >= 1 |
| F1-10 | `apps/prometheus/components/dashboards/storage-appliance.json` panel id 11 ("Active iSCSI sessions"); `kairos/node-agent/internal/metrics/metrics.go:412-413` (`countLIO`) and `:119` (`lioSessions` desc HELP string) | 1 | 3 | settled | verified | 2 blocking / 1 defer -> blocking (>= half at blocking-or-higher); both blocking voters wrote a consumer-naming consequence, so the rule-2 validation passes. DECIDING CONSEQUENCE (voter 2): "The operator verifying iSCSI after an appliance reboot or during a storage incident cannot tell whether any initiator is actually connected, because dashboard panel 11 titles `k2_lio_sessions` 'Active iSCSI sessions' and colours >=1 green while `metrics.go:412-413` counts persistent configfs ACL directories that democratic-csi's generate_node_acls/cache_dynamic_acls settings leave in place after every disconnect, and would instead read a green tile as confirmed connectivity and rule iSCSI out of the investigation while zero sessions are live." Voter 1 wrote the same shape for routine troubleshooting. This is a CLAIM OF FACT FALSE AGAINST SOURCE-CODE AUTHORITY that the consumer acts on. Verified end to end: `countLIO` increments `sessions` by `countDirs(<target>/<tpgt>/acls)` with the author's own comment conceding "configfs session state is deeper; ACL dirs are enough for the D28 boot/e2e signal"; `metrics.go:119` compounds it with HELP "LIO active session count."; `apps/democratic-csi/components/democratic-csi/secrets.ts:127-128` sets `generate_node_acls: 1` and `cache_dynamic_acls: 1`, the exact LIO configuration that auto-creates a NodeACL per connecting initiator and CACHES it in configfs rather than tearing it down with the session; the panel is titled "Active iSCSI sessions" with `colorMode: background` and steps red(base)/green(1). Cached ACLs are persisted to saveconfig.json and restored at boot, so the post-reboot state this panel exists to certify shows GREEN before any initiator has reconnected, and the red-at-0 alarm becomes structurally unreachable once the appliance has served a single PVC. The dissenting defer vote agrees on every fact and objects only that the underlying metric does serve the D28 first-boot signal and that the operator eventually finds out when a pod fails to mount — i.e. it argues the defect is the PROMISE the title/HELP/colour make, not the collector; that is a naming defect either way and both blocking voters agree a rename resolves it. NOT critical: reversible, and the appliance was probed live (k2.prom currently reports `k2_lio_sessions 0` with zero targets/LUNs), so the persistence behaviour is established from LIO/targetcli semantics rather than measured on this box. ROUND 2: reopened under its own policy (round 1 changed exactly panel 11 and the k2_lio_sessions HELP); 3/3 verified -> verified, the round-1 fix is CONFIRMED PRESENT AND CORRECT in the live text. All four recorded elements landed: panel 11 title is now "LIO node ACLs"; a description was added reading "Node ACL directories configured across all target TPGs. democratic-csi runs with cache_dynamic_acls, so an ACL persists after its initiator disconnects and survives reboot -- this is not a live session count."; fieldConfig.defaults.thresholds.steps reduces to a single neutral [{color:"text", value:null}] with options.colorMode "none", so the red-at-0/green-at-1 colouring that made green structurally unable to mean "connected" is gone; and metrics.go:119 now reads `lioSessions: &desc{"k2_lio_sessions", "LIO node ACL count; ACLs are cached, so this is not a live session count.", nil}`. The metric NAME and countLIO's ACL-directory source are unchanged exactly as the round-1 resolution deliberately recorded (renaming would break the already-deployed appliance textfile). Voters re-measured independently with python json dumps and direct file reads. RESIDUE booked separately, not re-litigated here: the design docs still carry the old "are workers actually attached" semantics (F2-12), countLIO's absent-root and descendant-read behaviour (F2-5, F2-6), panel 12's analogous over-claim (F2-7), and the row-mates' inherited colouring (F2-8). ROUND 3: proactively reopened (panel 11 was changed in round 2, the same round it was last voted); 3/3 verified again, checked in the live text rather than accepted on report. Panel 11 dumps as title 'LIO node ACLs', options.colorMode 'none', a single neutral threshold step [{color:'text', value:null}], description naming cache_dynamic_acls and stating 'this is not a live session count'; producer metrics.go:119 HELP 'LIO node ACL count; ACLs are cached, so this is not a live session count.' and countLIO :414 counts the `acls` directories. Title, colour, description and producer agree; the file still parses with exactly 30 panels, ids 1..30. The round-1 blocker is closed and stayed closed through two subsequent revisions. | verified — no action; the round-1 fix is in the live text | Reopen if panel 11's title/description/thresholds, the `k2_lio_sessions` metric name/HELP, `countLIO`'s source, or democratic-csi's `generate_node_acls`/`cache_dynamic_acls` settings change in a round >= 2 |
| F1-11 | `kairos/node-agent/internal/metrics/metrics.go:462-480` (`collectSMART`); `storage-appliance.json` panels 18, 19, 20, 23 | 1 | 1 | open | defer | 1 blocking / 2 defer -> defer (fewer than half at blocking-or-higher), though one defer voter records having actively considered blocking. FACT CONFIRMED: `collectSMART` initialises `result := groupResult{success: true}` and BOTH per-device bail-outs — a failed `smartctl -aj <dev>` at :467-470 and an unparseable/unsupported response at :472-475 — `continue` after only a `debugf`, leaving success untouched; the only `success: false` paths are on `--scan` itself. So a drive whose SMART reads have begun failing silently vanishes from panels 18/19/20 while panel 23 keeps reporting `smart = 1`. The blocking voter's consequence: "An operator relying on the SMART panels to catch a degrading drive before failure cannot do so once smartctl itself starts failing on that device ... and would instead have no signal at all for that drive rather than a visible 'device missing' indication." The two defer voters could not complete it: nothing the operator does next fails, it is a strict improvement over the pre-change state of no SMART visibility at all, the trigger is a narrow tail-end failure-to-QUERY case rather than the wear/error degradation the thresholds already catch through working reads, and a drive that actually drops out is independently reported by panel 2 (`k2_zfs_pool_health`, red base step). NAMED FUTURE COST: a health signal that fails open exactly on the failure it exists to catch — SMART is the one signal meant to fire BEFORE ZFS notices — with no expected-device set anywhere to notice the absence. | defer — set `result.success = false` on both `continue` paths, or emit a `k2_smart_devices` count the panels can compare against; best fixed once for the whole silence class with F1-2 (have `groupResult` treat an empty/degraded sample set as not-success) | Reopen if `collectSMART`'s per-device error handling, `groupResult`'s success semantics, or panels 18/19/20/23 change in a round >= 1 |
| F1-12 | `apps/prometheus/components/dashboards/storage-appliance.json` panels 29 ("K2 node builds") and 30 ("K2 node image checks") — no expected-node-set join | 1 | 1 | open | defer | 1 blocking / 2 defer -> defer (fewer than half at blocking-or-higher). FACT CONFIRMED from three independent panel dumps: both tables are driven purely by `max by (...)` over the k2_* series themselves, with no join against `kube_node_info`, `up`, or a hardcoded count — `grep -c 'up{'` over the dashboard returns 0 — so a node that never writes its textfile contributes no series, silently has no row, and no panel shows expected-versus-observed. Distinct from settled F0-11, which concerns the FORMAT of the node column, not row absence. All voters agree panel 30's WITHIN-row degradation is handled correctly (`imagecheck.go:286-303` emits `_check_success`/`_check_timestamp_seconds` unconditionally and gates only `_upgrade_available`/`_image_info` on success), but that mechanism needs at least one series for the instance — exactly what a fully-missing textfile defeats. The blocking voter's consequence: "A fleet operator cannot use panels 29/30 to confirm that all expected nodes are reporting ... and would instead believe the fleet is fully covered when a node has silently stopped reporting entirely." The two defer voters could not complete it: the fleet is 8 fixed-name boxes, the operator knows how many rows to expect and can count by eye, and the change is additive — no row is worse than the nothing that exists today. NAMED FUTURE COST: this is the exact blind spot settled F0-4 creates — a node whose k2-metrics unit failed at boot because /var/lib/prometheus/node-exporter did not exist yet disappears from the fleet table rather than appearing as a problem — so the fleet view is blind to the failure mode the ledger already booked. | defer — add a `count(k2_node_build_info)` vs `count(kube_node_info)` stat (or an outer join / absent-node panel); fix or accept together with F0-4 | Reopen if panels 29/30 gain an expected-node reference, or if F0-4's directory-creation gap is fixed, in a round >= 1 |
| F1-13 | `apps/prometheus/components/dashboards/storage-appliance.json` panel 29 target A (`max by (...)` grouping list); `kairos/node-agent/internal/metrics/metrics.go:104-108` (`nodeBuildInfo` desc) | 1 | 1 | open | defer | 1 blocking / 2 defer -> defer (fewer than half at blocking-or-higher). FACT CONFIRMED on both sides: the descriptor emits 11 labels (target, flavor, flavor_version, kairos_version, kairos_agent_version, kubernetes_distro, kubernetes_version, role, arch, hardware, source_commit) and panel 29 groups by 8, dropping `target`, `kubernetes_distro`, `arch` and `hardware`. `arch` and `hardware` are two of the four segments of the slot tag the image-check reconstructs (`<flavor-family>-<arch>-<hardware>-<role>`, imagecheck.go:156-165 aligned with plan.go tagSegments), and the fleet is deliberately heterogeneous (3 arm64 rpi4cb CP + 4 amd64 qemu workers + 1 amd64 qemu appliance). One voter adds that dropping `target` is the sharpest loss: that single label is the full descriptive target name and would have carried arch and hardware on its own, so the panel discards the cheapest possible fix too. The blocking voter's consequence: "An operator cannot use the 'K2 node builds' table to identify which image slot a given node is running ... unable to distinguish nodes that differ precisely in the labels that define slot identity." The two defer voters could not complete it: the table renders, every node still gets a row, and flavor/kairos_version/role answer the primary "is everyone on the same build?" question the panel exists for. NAMED FUTURE COST: anyone correlating panel 29 against panel 30's slot-keyed upgrade signal has to leave the dashboard — worse in combination with settled F0-11, where the 7 k8s rows are identified by nodeIP:9100 so arm64-rpi4cb cannot be inferred from the instance either. | defer — add `target` (or `arch` and `hardware`) to panel 29's `max by` list and to its organize transformation; one-line change, fix alongside F0-11 | Reopen if panel 29's grouping list or `nodeBuildInfo`'s label set changes in a round >= 1 |
| F1-14 | `kairos/node-agent/internal/imagecheck/imagecheck.go:300-302` (emission of `k2_node_image_info`); no consumer in `storage-appliance.json` or `dashboards/index.ts` | 1 | 1 | open | defer | 2 defer / 1 advisory -> defer. FACT CONFIRMED by repeated repo-wide grep: `k2_node_image_info{applied_digest,registry_digest,tag}` appears ONLY at imagecheck.go:300-302 (plus this ledger) — no dashboard panel, no recording rule, no alert consumes it. Distinct from settled F0-22, which verified the metric's label set and type, not whether anything reads it. Nobody is stopped: the metric IS emitted and scraped, so the applied-vs-registry digest pair is one Explore query away, and panel 30 already answers the routine operational questions (upgrade available, check succeeded, staleness). The advisory voter argued nothing predictable is lost for that reason; the two defer voters booked the future cost: panel 30 shows only the derived 0/1 bit, so the dashboard can say "this node is behind" but not WHICH digest it is on versus what the registry serves — precisely the two values needed when the bit looks wrong — and someone will eventually add it. | defer — surface `applied_digest`/`registry_digest` as two extra columns on panel 30 (nearly free if a reviser is already editing it); pair with F0-8, since the alert on `upgrade_available` and the info-labelled panel are the same missing consumer layer | Reopen if `k2_node_image_info`'s emission changes, or if a consumer (panel, rule or alert) is added, in a round >= 1 |
| F1-15 | `apps/prometheus/components/dashboards/storage-appliance.json` panels 5, 6, 7, 14, 18, 19, 21, 24; `kairos/node-agent/internal/metrics/metrics.go:592-604` (`parseRatio`), `:510-530` (`parseSMARTDevice`) | 1 | 1 | open | verified | 3/3 verified — affirmative result, independently re-measured by two voters rather than accepted on report, and covering ground settled F0-29 explicitly excluded (parse-validity only). `parseRatio` strips a trailing `%` and returns value/100, applied to zpool's frag (fields[3]) and cap (fields[4]) in `collectZFSPools:283-284`, so `k2_zfs_pool_fragmentation_ratio` and `k2_zfs_pool_capacity_ratio` are 0-1. Against that: panel 7 and panel 24 query the ratios DIRECTLY under `percentunit` with min 0 / max 1 and no second division (panel 24 even carries the description "Collector ratio is already 0-1; this query does not divide it again", matching the storage-observability design doc verbatim); panel 6 computes alloc/size, also 0-1, same unit; thresholds 0.75/0.9 and 0.5/0.8 are authored against a ratio, not a percent. `parseSMARTDevice` emits `nvme_smart_health_information_log.percentage_used`, `power_on_time.hours` and `temperature.current` UNSCALED, and panel 19 uses `percent` with min 0 / max 100 and thresholds 70/90, panel 21 uses `h`, panel 18 uses `celsius`. Panel 14 divides a seconds delta by 3600 under `h`; panel 5 uses `s` on a seconds delta. NO double-scaling and NO unit mismatch anywhere in the file. SCOPE: this verifies ratio/unit arithmetic only — it does NOT bless panel 5's behaviour when the timestamp is 0 (F1-3) or when the series is absent (F1-4). | verified — no action | Reopen if `parseRatio`/`parseSMARTDevice`'s scaling, or the units/thresholds on panels 5/6/7/14/18/19/21/24, change in a round >= 1 |
| F2-1 | `kairos/node-agent/internal/imagecheck/imagecheck.go:117-129` (marker branch of `check()`); authority `tools/internal/step/upgrade/upgrade_active.go:11-26`, `tools/internal/workflow/upgrade/upgrade.go:142-157`, `tools/internal/step/upgrade/verify_active.go:13-33` | 2 | 3 | settled | defer | 2 blocking / 1 defer -> blocking (>= half at blocking-or-higher); both blocking voters wrote a consumer-naming consequence, so the rule-2 validation passes. DECIDING CONSEQUENCE (voter 2): "An operator using panel 30 / k2_node_image_upgrade_available to decide which nodes still need upgrading cannot see a node that rolled back to the passive (old) image, because /usr/local/.state/k2-image-digest is written immediately after `kairos-agent upgrade` returns — before Reboot and Verify active image — and is never cleared or rewritten when those steps fail, and would instead read k2_node_image_upgrade_available 0 (\"up to date\") indefinitely for a node still running the old image." Voter 1 wrote the same shape for the fleet operator. Facts verified end to end by all three voters: imagecheck.go:123-129 returns `upgradeAvailable: appliedDigest != registryDigest` with no corroboration against what actually booted; `grep -rn AppliedDigestPath tools/` finds exactly one writer (upgrade_active.go:25) and it runs inside the upgrade step; workflow/upgrade.go orders upgrade (:142) -> Reboot (:147) -> Verify active (:152); verify_active.go:19-21 errors "node booted into %q mode, not active; manual recovery required" and touches no file; nothing anywhere removes the marker; a passive/rolled-back boot does not create /run/cos/recovery_mode so the unit's ConditionPathExists does not suppress the run. NOT critical: the failed upgrade that creates the stale marker is itself reported loudly by k2-tools at the time, and k2_node_build_info (panel 29) still carries the booted image's versions as an independent same-dashboard cross-check. The dissenting defer vote rests entirely on that loudness; the two blocking voters answer that the metric then lies for the LIFE of the node — unlike every settled wrong-healthy finding, this one never self-heals. The second sub-case (`plan.Target.Digest == ""` skips the marker write, leaving the previous upgrade's digest) errs toward a false upgrade_available 1 — noisy, not silent — and is NOT what carries the severity. ROUND 3: reopened under its own policy (the marker WRITE SITE changed in round 2); 3/3 defer -> defer. THE DIRECTED FIX IS CONFIRMED PRESENT: `RecordAppliedDigest` is now its own Runner method (upgrade_active.go:29-36) invoked from a workflow step at upgrade.go:163-166 placed AFTER `Verify active image` (:152-158), and ui.Workflow.Execute aborts at the first failing step, so every path from Cordon through VerifyActive leaves the marker byte-identical. The round-2 harm — a rolled-back node reporting upgrade_available 0 for the life of the node — is discharged: such a node now keeps its OLD digest and reports 1 (loud, correct). RESIDUE, deferred: `grep -rn AppliedDigestPath tools/` still shows writers only at upgrade_active.go:31/:35 and a single reader at workflow/upgrade/metadata.go:37, so nothing invalidates or repairs a marker that disagrees with the running image, and imagecheck.go:117-133 still returns `appliedDigest != registryDigest` without cross-checking the sourceCommit it already read at :100 (the second remediation option this ledger offered). A marker left over from pre-fix code is hypothetical for the current fleet (the appliance's sha256:7474dd79 matches its running image per docs/design/node-metrics.private.md:303-305). The inverse case — old marker after a failed Verify — is booked as F3-1 blocking and is not double-counted here. | defer — the directed fix landed; optionally add marker/sourceCommit corroboration in imagecheck so a wrong marker can self-heal | Reopen if the marker write site moves again, if an invalidation/repair path is added, or if imagecheck.go's marker branch changes, in a round >= 4 |
| F2-2 | `kairos/node-agent/internal/imagecheck/imagecheck.go:196-218` (fetchRevision single-manifest early path); `internal/imagecheck/imagecheck_test.go:76-104` and `:195-203` (registry fixture) | 2 | 2 | open | defer | 3/3 defer. The fact is confirmed: imagecheck_test.go:83-84 runs the only wantUpgrade-false subtest (`match`) with `index: false`, so the fixture at :195-203 serves a bare image manifest and exercises the `manifest.Config.Digest != ""` early path, while a live measurement (anonymous ghcr pull token, the code's exact three-type Accept header) shows all three published slot tags — ubuntu-amd64-qemu-storage, ubuntu-arm64-rpi4cb-k8s, ubuntu-amd64-qemu-k8s — return `application/vnd.oci.image.index.v1+json`. So production always takes the index branch and the operator-visible steady state (index shape AND commits equal AND upgrade_available 0) is the one combination no test covers. This is the severity model's own example of defer, "coverage not yet written": nobody is stopped, the index traversal IS exercised by the `mismatch through OCI index` subtest, and the two branches converge on the same `manifest.Config.Digest`/label read, so the untested combination differs by one boolean comparison. FUTURE COST: the production-shape no-upgrade direction has no regression guard, so a later change to fetchRevision's index branch could ship green. Distinct from settled F0-26, which verified the accept headers and live index shape, not the test matrix. | defer — one-line fix: flip the `match` subtest to `index: true`, or add a third subtest covering index + equal commits | Reopen if fetchRevision's branching, the test matrix, or the published manifest shape of the slot tags changes in a round >= 2 |
| F2-3 | `kairos/node-agent/internal/imagecheck/imagecheck.go:135-151`; `docs/design/node-metrics.private.md` §3 step 5 (:210-214); `kairos/Dockerfile:58` and `:250`; `.github/workflows/kairos-image-build.yaml` | 2 | 2 | open | defer | 3/3 defer. Confirmed: imagecheck.go:148 decides on `registryCommit != sourceCommit`, and both sides trace to a single `git rev-parse HEAD` at build time (tools/internal/workflow/image/image.go:143-152 -> SOURCE_COMMIT build arg -> Dockerfile:58 `org.opencontainers.image.revision` label AND :250 metadata.yaml `sourceCommit`), so a rebuild at unchanged HEAD publishes a NEW index digest with an UNCHANGED revision and the fallback reports upgrade_available 0 for it. workflow_dispatch is an exposed trigger, and per settled F0-20 raw-flashed nodes never have the marker so the fallback is their steady state. The design doc specifies the mechanism verbatim with NO caveat while tools/internal/step/upgrade/upgrade_active.go:18-19 already carries one for k2-tools. Defer because no consumer's next action fails: the coarseness under-reports rather than fabricating an upgrade, it applies only where the marker is absent, and it self-corrects on the next commit-bearing build. FUTURE COST: a doc reader assumes digest-exact semantics from the fallback, and a raw-flashed node silently misses a same-commit rebuild. | defer — one sentence in `docs/design/node-metrics.private.md` §3 step 5 mirroring the caveat already at upgrade_active.go:18-19; no code change required | Reopen if the fallback comparison, the SOURCE_COMMIT plumbing, or the design doc's §3 step 5 text changes in a round >= 2 |
| F2-4 | `kairos/node-agent/internal/metrics/metrics.go:21-23` and `:88-89` (doc comments); `apps/prometheus/components/dashboards/storage-appliance.json` (whole file) | 2 | 2 | open | defer | 1 blocking / 2 defer -> defer (fewer than half at blocking-or-higher). The FACT is unanimous and confirmed: `grep -rn node_textfile .` excluding node_modules/.git/deploy returns exactly metrics.go:23, metrics.go:89 and this ledger — nothing in the repo consumes node_textfile_mtime_seconds despite two doc comments asserting freshness "is monitored via" it; the dashboard contains 0 occurrences of `up{`; the only in-tree consumer is the vendored NodeTextFileCollectorScrapeError at deploy/prometheus/app.k8s.yaml:12417 pinned to `job="node-exporter"`, which cannot match jobName "storage-appliance" (settled F1-1). node_exporter's textfile collector has no TTL, so a stopped k2-metrics writer freezes 20 of the 22 k2.prom-driven panels at their last values — pool ONLINE, key available, storage healthy, all eight collector_success, SMART — and panel 15's absent() guard sees a present-but-frozen series and renders 0/green. WHAT DEMOTED IT: both defer voters checked the mechanism and found an independent signal the finding did not credit — collectStorageHealth sources k2_storage_health_last_run_timestamp_seconds from the status file's ModTime, so when the writer stops, panel 5's `time() - ...` climbs past its 7200s red step within two hours on its own (panel 14 likewise for snapshot age). The operator does get a red tile without acting on the false comment. FUTURE COST: 20 panels give false reassurance in the interim, and the false comment will steer whoever writes the alerting rules away from adding the staleness alert that would cover it. Book with settled F0-8 (no author-written alerts) and F1-1. | defer — correct the two doc comments to say freshness is NOT yet monitored, and add a textfile-staleness alert (with F0-8) plus a scrape-liveness tile (with F2-9) | Reopen if either doc comment changes, if any consumer of node_textfile_mtime_seconds is added, or if alerting rules land, in a round >= 2 |
| F2-5 | `kairos/node-agent/internal/metrics/metrics.go:390-397` (countLIO os.IsNotExist branch), `:369-388` (collectLIO), `:434-438` (saveConfigInSync); storage-appliance.json panels 9, 10, 11, 12, 23 | 2 | 2 | open | defer | 1 blocking / 2 defer -> defer. Fact confirmed verbatim by all three: `if os.IsNotExist(err) { return 0, 0, 0, nil }` at metrics.go:390-395, and collectLIO's ONLY failure gate is a non-NotExist error, after which it hard-codes `success: true` — so an absent /sys/kernel/config/target/iscsi yields k2_lio_targets 0, k2_lio_luns 0, k2_lio_sessions 0, k2_lio_saveconfig_in_sync 1 and k2_collector_success{collector="lio"} 1, byte-identical to healthy-and-idle. LIVE MEASUREMENT 2026-08-10 against 10.10.9.250:9100 confirms the appliance already sits at that indistinguishable point. This is a genuinely NEW site of the recurring absent-reads-as-healthy theme, at a location neither settled F1-2 (ZFS collectors) nor F1-11 (SMART) covers, and worse in kind because it FABRICATES zero samples rather than emitting none. Demoted to defer on the same calibration those two received, and for a checked reason: the hidden state (target_core_mod unloaded / configfs unmounted) is not quiet — every iSCSI PVC attach fails and workers wedge in ContainerCreating, which the operator meets through the workload long before this dashboard; and after the round-1 fix panels 9/10/11 are uncoloured counts where 0 and No Data read alike. The one panel that renders actively green in this state is panel 12, booked separately and BLOCKING as F2-7. FUTURE COST: an operator debugging attach failures wastes a pass on a green LIO row. | defer — propagate the IsNotExist as an error (or emit no LIO samples at all when the root is absent) so "LIO gone" and "LIO idle" differ; fix together with F2-6 and F2-7 | Reopen if countLIO's absent-root handling, collectLIO's success gate, or the LIO panels' colouring change in a round >= 2 |
| F2-6 | `kairos/node-agent/internal/metrics/metrics.go:407-431` (countLIO inner loop, countDirs), `:369-388`, `:153-160`; storage-appliance.json panel 23 | 2 | 2 | open | defer | 3/3 defer. Confirmed as a mechanism DISTINCT from F2-5's absent-root case: `countDirs` at metrics.go:419-424 does `entries, err := os.ReadDir(path); if err != nil { return 0 }` and returns no error, while the sibling tpgt-level ReadDir at :401-404 correctly does `return 0, 0, 0, err` — so the swallowing is specific to the two call sites at :411 (lun) and :414 (acls), and collectLIO still emits k2_collector_success{collector="lio"} 1 alongside the fabricated zero, contradicting panel 23's own description "A failed collector stays visible even when the metrics it owns disappear." Defer because the trigger is narrow and self-correcting: both writer units run as root (no User=/Group=/DynamicUser= on k2-metrics.service), so a permission failure on configfs is effectively unreachable, and a mid-scan TPG teardown produces one wrong sample the next 60s run replaces. FUTURE COST: an inconsistent error contract inside one function that will confuse whoever next tries to make the LIO collector honest about failure. | defer — return the error from countDirs rather than 0, matching the tpgt-level handling one level up; fix alongside F2-5 | Reopen if countDirs' error handling, countLIO's inner loop, or panel 23's description change in a round >= 2 |
| F2-7 | `apps/prometheus/components/dashboards/storage-appliance.json` panel 12 (lines 602-655); `kairos/node-agent/internal/metrics/metrics.go:120` (lioSaveInSync desc), `:390-395`, `:434-445` (saveConfigInSync) | 2 | 3 | settled | verified | 2 blocking / 1 defer -> blocking (>= half at blocking-or-higher); both blocking voters wrote a consumer-naming consequence, so rule-2 validation passes. DECIDING CONSEQUENCE (voter 2): "An operator using panel 12 to confirm the appliance's live LIO configuration is persisted cannot detect a drifted saveconfig, because the producer compares only the NUMBER of live target directories against len(saveconfig.targets) and returns 1 when both configfs and saveconfig are absent, and would instead see a green background 'LIO saveconfig in sync' tile and discover the mismatch only after a reboot restores a different (or empty) target set and PVCs fail to attach." Voter 1 wrote the same shape. Facts verified on both sides: panel 12 dumps as title "LIO saveconfig in sync", NO description, unit bool, thresholds [red@null, green@1], options.colorMode "background" — so 1 paints the whole tile green; metrics.go:443-445 is `return len(parsed.Targets) == liveTargets`, comparing no target identities, TPGs, LUNs, ACLs or settings, and :435-437 returns `os.IsNotExist(err) && liveTargets == 0`, i.e. green when BOTH configfs and saveconfig are missing (the F2-5 state). The HELP at metrics.go:120 ("Whether live LIO target count matches saveconfig.") is honest; the dashboard, which is what the operator reads, is not. This is the same shape the ledger already resolved blocking as F1-10 — a title plus green colouring asserting a guarantee the producer does not provide, with no description to fence it — and it guards precisely the reboot-restoration path that kairos/overlays/role/storage/oci/system/oem/10-storage-services.yaml exists to make work. NOT critical: the wrong outcome surfaces at the next reboot and the config can be re-created. The dissenting defer vote agrees on every fact and argues only that, unlike F1-10, green DOES mean in-sync in the ordinary steady state and red is reachable on any count drift. ROUND 3: proactively reopened (panel 12 and saveConfigInSync were both changed in round 2, the round this was last voted); 3/3 verified -> verified. The correction is present in the live text. Panel 12 title is now 'LIO target count matches saveconfig' — literally what saveConfigInSync computes (metrics.go:451 `return len(parsed.Targets) == liveTargets`) — and every clause of the new description checks out against the producer: count-only comparison with identities/TPGs/LUNs/ACLs excluded, same-count drift still reading 1, and 0 also meaning missing-or-unparseable (metrics.go:441-448 returns false on both read and unmarshal error). The green-at-1 background no longer asserts a guarantee the producer does not provide, because the title no longer claims 'in sync'. The both-absent case the round-2 resolution asked about is now false-on-read-error; the remaining unstated nuance — 1 is also returned when both sides are zero — is covered literally by the title and is the appliance's actual measured state (saveconfig.json present, 69 bytes; k2_lio_targets 0; k2_lio_saveconfig_in_sync 1). A panel-shape audit across all data panels (2-7, 9-12, 14-16, 18-21, 23-24, 26-27, 29-30) found no remaining title/colour guarantee beyond its producer: THE RECURRING DASHBOARD OVER-CLAIM SHAPE IS EXHAUSTED. | verified — no action; the round-2 fix is in the live text and the panel-shape class is exhausted | Reopen if panel 12's title/description/thresholds or saveConfigInSync's comparison change in a round >= 3 |
| F2-8 | `apps/prometheus/components/dashboards/storage-appliance.json` panel id 9 ("LIO targets", line 464) and id 10 ("LIO LUNs", line 506) | 2 | 2 | open | defer | 1 advisory / 2 defer -> defer (>= half at defer-or-higher). Confirmed by per-panel dumps: 9 and 10 are the ONLY panels in the file whose fieldConfig.defaults is exactly `{"unit": "short"}` while options.colorMode is "value", so both inherit Grafana's default green-base absolute scale and a No-Data placeholder paints green; panel 11 beside them now carries an explicit single neutral `text` step with colorMode "none" after the round-1 fix, and the pre-existing in-repo dashboard kura-suite-overview.json panel 2 authors exactly that neutral convention for a count tile. Not higher because colour on a raw count tile carries no semantic contract — an operator does not read a green "3" on "LIO targets" as a health verdict the way they read panel 12's background — and the settled green-base-on-absence finding F1-4, where green genuinely means healthy, landed defer. FUTURE COST: the round-1 revision established a neutral convention on panel 11 and left its two row-mates on the inherited default, so the iSCSI row renders three sibling counts three different ways and will keep drifting. | defer — two small JSON additions mirroring panel 11's neutral single-step thresholds; fold into the one convention decision tracked by F2-9 | Reopen if panels 9/10 gain colour or threshold config, or if the dashboard's absence-rendering convention is settled, in a round >= 2 |
| F2-9 | `apps/prometheus/components/dashboards/storage-appliance.json` — threshold base steps on panels 6, 7, 18, 19 (green base, no noValue) and the file-wide absence of any `up` query | 2 | 2 | open | defer | 1 blocking / 2 defer -> defer. The enumeration was independently re-measured and holds: panel 6 [(green,null),(yellow,0.75),(red,0.9)], panel 7 [(green,null),(yellow,0.5),(red,0.8)], panel 19 [(green,null),(yellow,70),(red,90)] all with noValue unset; `absent(` occurs only in panel 15, `noValue` only in panel 26 ("No zvol PVCs"), and `up{` occurs ZERO times across 30 panels — the appliance dashboard has no scrape-liveness indicator at all. Absence is decided per panel with no shared rule (red base on 2/3/4/12/23, absent() on 15, noValue on 26, green base on 5/6/7/18/19/20 plus 9/10 by inheritance, palette-classic on 11/14/16/21/24, tables 27/29/30 with nothing). ONE CORRECTION to the finding's evidence, from voter 3: panel 18 is a timeseries with palette-classic colouring, so its threshold steps are vestigial and it renders a bare "No data" rather than a healthy colour — the uncovered green-base set is 6, 7 and 19. Correctly scoped away from settled F1-4 (panels 5 and 20), and lands at the same severity F1-4 got: nobody is stopped, every panel renders a real value whenever the series exists, and TargetDown still fires on unreachability. FUTURE COST: each new panel re-litigates absence semantics, and drift is guaranteed. | defer — settle ONE dashboard-wide absence convention (neutral base + noValue text, absent() where a boolean must alarm) and add a scrape-liveness tile; book with F1-4, F1-12, F0-8 and F2-8 as a single decision | Reopen if the dashboard's threshold/noValue conventions change, if a liveness panel is added, or if panels 6/7/19 are re-coloured, in a round >= 2 |
| F2-10 | `kairos/node-agent/internal/metrics/metrics.go:123` (smartMediaErrors desc), `:513-550` (parseSMARTDevice); storage-appliance.json panel 20 | 2 | 2 | open | defer | 3/3 defer. Confirmed at metrics.go:519-530 and :541-547: the struct declares BOTH `nvme_smart_health_information_log.media_errors` and `ata_smart_error_log.summary.count`, and both branches append to `c.smartMediaErrors` with the same `device` label, against a HELP of "SMART media error count." and a panel titled "Media errors" with steps [(green,null),(red,1)]. The local smartctl(8) manual (7.5, :1338-1410) defines the ATA summary log as the most recent non-trivial errors spanning command, interface, addressing AND media classes (ABRT, ICRC, IDNF, UNC), and notes some vendors log invalid or unimplemented commands — so on the appliance's SATA spinners the family is not a media-error count. Defer, and notably the ONE finding in this batch that errs in the SAFE direction: it fails loud (false red), not silent green, and the operator's response — run smartctl on the flagged drive — resolves the ambiguity in one command on a drive that has in fact logged a non-trivial SMART error. The duplicate-label-set concern raised by the collector is theoretical and was rejected by two voters: smartctl emits nvme_smart_health_information_log for NVMe and ata_smart_error_log for ATA, never both for one device. FUTURE COST: the family means two different things depending on transport, and any alert written on it later inherits the confusion. | defer — reword the HELP and panel title, or split the ATA summary count into its own metric family | Reopen if the SMART parsing branches, the smartMediaErrors HELP, or panel 20's title/thresholds change in a round >= 2 |
| F2-11 | `apps/prometheus/components/dashboards/storage-appliance.json` panel 26 description (line 1249) and panel 27 description (line 1312); `apps/prometheus/components/dashboards/index.ts` `storageZvolPvcRecordingRule()`; `kairos/node-agent/internal/metrics/metrics.go:365` | 2 | 2 | open | defer | 1 blocking / 2 defer -> defer. Both halves confirmed: panel 26 says "No data means no matching k2-iscsi PVC exists" and panel 27 "No data means no zvol-backed PVCs exist", while both read ONLY the recorded k2_zfs_volume_pvc_* families produced by a vector multiplication against kube_persistentvolumeclaim_info — so an unscraped appliance target, a failed zfs_volumes collector or stopped writer, absent kube-state-metrics, the settled F1-9 many-to-many abort, and the post-restart window before the rule's first evaluation ALL yield the identical empty result; the descriptions name one of five. `grep -rn "k2_zfs_volumes" apps/` returns ZERO hits while metrics.go:365 appends that series unconditionally on every run — the one metric that would separate "zero zvols" from "no data" is emitted and consumed by nobody, which two voters called the finding's best catch. Defer because panel 26 already renders an honest "No zvol PVCs" placeholder rather than a healthy-coloured value, the appliance's real state today IS zero zvol PVCs, and three of the five confounders have independent signals elsewhere (TargetDown, panel 23, panel 5). This is defer's own example, "prose that will mislead a later reader." FUTURE COST: an operator concludes no PVCs exist when the join is broken and stops investigating. | defer — soften both descriptions to name the confounders, and put the unconsumed `k2_zfs_volumes` on a tile beside panels 26/27 so zero-zvols and no-data are distinguishable | Reopen if panel 26/27 descriptions, the recording rule, or `k2_zfs_volumes` consumption change in a round >= 2 |
| F2-12 | `docs/design/storage-observability.private.md:225-226` (Phase D); `docs/design/storage-appliance.private.md:690` — vs `kairos/node-agent/internal/metrics/metrics.go:119` and storage-appliance.json panel 11 | 2 | 2 | open | defer | 1 blocking / 2 defer -> defer. Grep-confirmed by all three: storage-observability.private.md:225-226 still reads "`k2_lio_sessions` (sessions is the \"are workers actually attached\" signal)" and storage-appliance.private.md:690 still lists "active iSCSI sessions", with no correction anywhere in docs/design/*.md — directly contradicting the round-1 corrections at metrics.go:119 and panel 11's new description, which say in as many words that this is NOT a live session count. This is the residue of the F1-10 fix and the highest-value defer in the batch: a Phase D implementer building the attach gate from the design doc would gate on a cached counter that stays >0 forever after the first attach, i.e. a test gate that can never fail. Defer rather than blocking on the model's own terms — the consumer stopped by a defect must be the one who acts on THIS artifact next (the implementer/committer, the operator), and a future-phase reader of an unchanged gitignored design doc is exactly the "prose that will mislead a later reader" case; nothing a consumer does today fails, since the code and dashboard are correct and only the doc is stale. Voter 3 adds that metrics.go:412's own comment still endorses ACL dirs "for the D28 boot/e2e signal", so code and doc agree on the intended USE and only the operator-facing surfaces were corrected. | defer — two-line doc edit in both design docs saying k2_lio_sessions counts cached node ACLs, and stating explicitly what D28 may and may not conclude from it, so the settled F1-10 resolution cannot be undone by a stranger reading the authority | Reopen if either design doc's k2_lio_sessions text changes, or if panel 11 / metrics.go:119 semantics change again, in a round >= 2 |
| F2-13 | `kairos/node-agent/internal/metrics/metrics_test.go` — TestRenderStorageRoleIncludesStorageCollectors (:312-338); fakeRunner at `:561-573` | 2 | 2 | open | defer | 3/3 defer. Confirmed: fakeRunner.Output returns `r.outputs[key], r.outErrs[key]`, i.e. ("", nil) for any unmapped command, and the test maps only `smartctl --scan -j` while asserting `k2_collector_success{collector="<name>"} 1` for all eight groups including zfs_pools, zfs_keystatus, zfs_volumes, zfs_snapshots, lio and storage_health. It passes only because those collectors initialise `groupResult{success: true}` ahead of their loops (settled F1-2) plus the LIO absent-root branch (F2-5) and collectStorageHealth's IsNotExist branch (:486-496) — so the empty-means-success behaviour is now pinned in TEST text at a location never voted on before. Defer: the test's actual purpose, role gating, is correct and served, and the suite is green so no consumer's next action is affected. FUTURE COST, precise and worth booking: whoever fixes F1-2/F2-5/F2-6 to report failure on empty output turns this test red and must decide whether the redness is the fix working or a regression. The state it describes — a storage node where every zpool/LIO/SMART/health query returns nothing — corresponds to no configuration of the real appliance. | defer — narrow the assertion to the collector NAMES/metric families being emitted rather than their success VALUES, so the role-gating test decouples from the empty-output semantics | Reopen if the test's assertions, fakeRunner's defaults, or the collectors' empty-output success behaviour change in a round >= 2 |
| F2-14 | `kairos/node-agent/internal/buildmetadata/metadata.go:14-38`; generator authority `kairos/Dockerfile:237-253` | 2 | 2 | settled | verified | 3/3 verified. The parser was checked against its ONLY producer rather than in the abstract, which is what bounds it: metadata.go:20-33 trims the line, `strings.Cut`s on the first colon, trims the value, skips colon-less lines and keeps a key whose value is empty — so CRLF parses identically (bufio.ScanLines strips the CR), " flavor : ubuntu-26.04 " yields flavor/ubuntu-26.04, `printf 'role: %s\\n' ""` keeps the key with an empty value (which slotTag's TrimSpace-empty check at imagecheck.go:159-162 and collectNodeBuild's `metadata["target"] == ""` check both rely on), and duplicate keys take the last. Dockerfile:238-253 emits thirteen flat `printf '<key>: %s\\n'` lines with no nesting, comments, quoting or colon-bearing values and nothing near bufio's 64 KiB token limit, so no residual ambiguity is reachable from the generator. The one distinction the parser cannot express — key-absent vs key-present-but-empty — is treated identically by both callers by design. Padded-key and empty-value cases are already pinned by existing tests in metrics_test.go. Complements settled F1-7 (no test file in this package) without re-raising it. | verified — no action | Reopen if buildmetadata's parser, the Dockerfile metadata generator, or either consumer's empty-value handling changes in a round >= 2 |
| F2-15 | `kairos/node-agent/internal/textfile/textfile.go:22` (Chmod); rendered `deploy/prometheus/app.k8s.yaml:8802, 8834-8836, 8846-8849, 8853-8858, 8875-8878`; both role overlays' k2-metrics/k2-image-check units | 2 | 2 | settled | verified | 3/3 verified. The cross-process chain was re-checked link by link: textfile.go:22 does `tmp.Chmod(0o644)` before the rename; both k2-metrics.service and k2-image-check.service (read directly in both role overlays) contain only Description/ConditionPathExists/Type=oneshot/ExecStart with NO User=, Group= or DynamicUser=, so both oneshots run as root and the .prom files land root-owned world-readable; the rendered node-exporter DaemonSet runs as uid/gid 65534 with runAsNonRoot and fsGroup 65534 and mounts /var/lib/prometheus/node-exporter readOnly from a hostPath, and readOnlyRootFilesystem does not affect a mounted volume — the other-read bit satisfies every link. Distinct from settled F0-31, which verified the atomic-rename sequence rather than the mode/uid chain. The finding honestly flags the one link the artifact does not set — the directory's own mode, left to kubelet's `type: DirectoryOrCreate` — and correctly points at settled F0-4 rather than double-booking it. | verified — no action | Reopen if textfile.Write's mode, either unit's User=/Group=, or the DaemonSet's securityContext/volume mount changes in a round >= 2 |
| F2-16 | `apps/prometheus/components/dashboards/storage-appliance.json` panels 9-10 (lines 463-540); `kairos/node-agent/internal/metrics/metrics.go:117-118`, `:398-414` (countLIO) | 2 | 2 | settled | verified | 3/3 verified. Both directions checked: panel 9 is titled "LIO targets" querying k2_lio_targets and panel 10 "LIO LUNs" querying k2_lio_luns; metrics.go:117-118 give HELP "LIO iSCSI target count." and "LIO LUN count."; countLIO increments `targets` only for `iqn.`-prefixed directories (:398-402) and counts `lun_`-prefixed directories under each `tpgt_` entry (:407-411). For READABLE configfs the names match the measurement exactly, so the F1-10 naming defect does not extend to panel 11's row-mates. The self-imposed scope is what makes this useful and all three voters endorsed it: it is conditioned on "readable", which is exactly what F2-5 and F2-6 put in question, and it says nothing about colouring (F2-8). | verified — no action | Reopen if panels 9/10's titles or queries, the lioTargets/lioLUNs HELP strings, or countLIO's counting rules change in a round >= 2 |
| F2-17 | `kairos/node-agent/internal/metrics/metrics.go:108-116, 127-128` (ZFS descriptors), `:269-367`, `:589-598`, `:625-681` (collectors); storage-appliance.json panels 2, 3, 6, 7, 14, 16, 24, 26, 27 | 2 | 2 | settled | verified | 3/3 verified. Semantic (not unit) name-to-computation check of the ZFS families, spot-checked at several independent points: metrics.go:270 requests `name,size,alloc,frag,cap,health` and :282-300 maps them with `fields[5] == "ONLINE"` as the only 1-value for k2_zfs_pool_health (HELP "ZFS pool health, 1 when ONLINE."); parseRatio (:593-598) divides frag/cap by 100 for the _ratio families; keystatus maps only `available` to 1 (:314-333); zvols map volsize/used (:340-365); the snapshot collector (:625-681) uses maximum cadence creation time per prefix for k2_zfs_last_snapshot_timestamp_seconds and distinct cadence suffixes for k2_zfs_snapshot_count. Each matches its descriptor text and its panel title/query. Correctly scoped away from settled F1-15 (ratio/unit arithmetic) and from F1-2/F2-9 (what these panels render when the series is ABSENT), neither of which is re-litigated here. | verified — no action | Reopen if the ZFS collectors' field mapping, the ZFS descriptor HELP strings, or the corresponding panel titles/queries change in a round >= 2 |
| F2-18 | `apps/prometheus/components/dashboards/storage-appliance.json` panel 5 description and panel 30 description + field overrides; `kairos/overlays/role/storage/oci/etc/systemd/system/k2-storage-health.timer`; `kairos/node-agent/internal/imagecheck/imagecheck.go:286-303` | 2 | 2 | settled | verified | 3/3 verified. Two panel-description claims re-checked against their authorities and BOTH hold — reported as the deliberate contrast case to F2-11's descriptions that do not. (1) Panel 5 says "The health job runs hourly; red means its last result is more than two hours old": k2-storage-health.timer reads `OnBootSec=2min` / `OnUnitActiveSec=1h` and the panel's threshold dump is [(green,null),(yellow,5400),(red,7200)] with unit "s" — both halves true. (2) Panel 30 says "a missing upgrade value after a failed check is not treated as up to date": imagecheck.render() emits k2_node_image_upgrade_available and k2_node_image_info ONLY inside `if result.success`, while check_success and check_timestamp_seconds are unconditional, so a failed check leaves the upgrade cell empty beside a red check-succeeded cell — matching docs/design/node-metrics.private.md:176-178 ("write `check_success 0` and **omit** `upgrade_available` ... a fabricated 0 shows as \"healthy\""). Correctly scoped away from settled F1-3 (never-ran sentinel) and F1-4 (green base step). | verified — no action | Reopen if panel 5/30 descriptions or thresholds, the storage-health timer cadence, or imagecheck.render()'s success gating change in a round >= 2 |
| F3-1 | `tools/internal/workflow/upgrade/upgrade.go:108, :126-150, :163-166`; `tools/internal/step/upgrade/resolve.go:133-150`, `:302`; `tools/internal/step/upgrade/preflight.go:13-20`; `tools/internal/ui/workflow_run.go:61-63` | 3 | 3 | open | blocking | 3/3 blocking; all three wrote a consumer-naming consequence, so rule-2 validation passes. DECIDING CONSEQUENCE (voter 3): "An operator re-running `k2-tools upgrade <host>` after the workflow failed at `Verify active image` cannot get the resume path, because the round-2 revision writes the applied-digest marker only after Verify succeeds while `ActiveImageMatchesTarget` short-circuits on the digest branch (old marker digest vs new target digest) and never reaches the source-commit fallback that would have matched, and would instead be walked through a second Cordon -> Drain -> `kairos-agent upgrade` -> Reboot of a node that is already running the target image." Voter 2 adds the sharper tail: `preflight.go:13-20` gates BOTH the quorum and the COS_STATE free-space refusals on `!ActiveImageMatchesTarget()`, so on a degraded control plane or a tight COS_STATE the re-run is REFUSED outright with the node left cordoned and drained and no tool path to finish. THIS IS A REGRESSION INTRODUCED BY THE ROUND-2 FIX: under the committed pre-fix code the marker was already the new digest at that point and resume mode engaged. Verified end to end: upgrade.go:108 `resumePostUpgrade := plan.ActiveImageMatchesTarget()` with `.Unless(resumePostUpgrade)` on Cordon/Drain/UpgradeActive/Reboot (:133/:140/:145/:150); the new `Record applied image digest` step at :163-166 sits after `Verify active image` at :152-158; ui/workflow_run.go:61-63 returns at the first failing step; resolve.go:142-145 returns the digest comparison whenever both digests are non-empty, making the CurrentSourceCommit/TargetSourceCommit branch at :146-148 unreachable for any marker-bearing node. resolve.go:133-137's own doc comment states the resume contract this breaks. Markers are not hypothetical — one voter read `sha256:7474dd79a1c6...` out of /usr/local/.state/k2-image-digest over SSH this session. Nodes with no marker are unaffected. NOT critical: the Plan/Confirm prompt shows 'full upgrade' and the workflow runs loudly, and re-applying the same image is reversible — the operator does find out, but only after a needless drain+reboot. | fix — compare source commits when the digest branch disagrees (or fall through to the sourceCommit fallback instead of short-circuiting), and/or repair the stale marker, rather than relying on write-ordering alone | Reopen if `ActiveImageMatchesTarget`'s branch precedence, the marker write position, or preflight's resume gating changes in a round >= 3 |
| F3-8 | `tools/internal/step/upgrade/resolve.go:413-421` (tagPrefix), `:283-295`; `tools/internal/client/oci/discover.go:181-194`; `kairos/node-agent/internal/imagecheck/imagecheck.go:154-165` (slotTag); `tools/internal/image/plan/plan.go:430-445` | 3 | 3 | open | blocking | 2 blocking / 1 defer -> blocking (>= half at blocking-or-higher); both blocking voters wrote a consumer-naming consequence, so rule-2 validation passes. DECIDING CONSEQUENCE (voter 3): "An operator acting on the new `k2_node_image_upgrade_available 1` signal after a base or Kairos bump cannot upgrade the node with `k2-tools upgrade <host>`, because auto-discovery filters registry tags by `tagPrefix(meta)` — which embeds the node's CURRENT flavor release and Kairos version — while the build the node's image-check watches is published under the version-free floating slot tag, and would instead be offered the newest OLD-version build (typically the node's own image, flipping the workflow into 'resume post-upgrade' mode) and finish with a green 'Upgrade complete' banner naming the image the node was already running." Voter 1 wrote the same shape. Verified against source AND the live registry: resolve.go:286 passes `tagPrefix(meta)` to DiscoverLatest; upgrade_test.go:14-25 pins it to 'ubuntu-26.04-v4.1.2-arm64-rpi4cb-k8s-'; discover.go:187-190 matches by `strings.HasPrefix` only; imagecheck.go:157-164 reconstructs slotTag = flavorFamily-arch-hardware-role. A live `tags/list` (34 tags) shows both namespaces published, and the floating tags do NOT match the descriptive prefix. THE REPO IS ITS OWN AUTHORITY: the comment this very diff EXTENDS at plan.go:436-441 says versions were removed from tagSegments precisely because 'a base or Kairos bump left every node searching for the version it already ran', and adds 'Keep this aligned with slotTag in kairos/node-agent/internal/imagecheck/imagecheck.go' — while resolve.go's parallel tagPrefix still does the thing that comment condemns. Distinct from settled F0-24, which verified the slotTag<->tagSegments pair only; the unaligned THIRD site is resolve.go's imageTagBase. SCOPE CORRECTION from voter 1: a k3s bump is NOT affected (KubernetesVersion is not in imageTagBase, and the prefix ends before it) — the reported k3s portion is overstated; the base/Kairos case is what carries the severity. The dissenting defer vote notes resolve.go is unchanged committed code; the majority answers that THIS diff is what makes the divergence consequential by shipping the node-side signal whose only sanctioned follow-through is the tool that cannot resolve the same slot, and that the operator's decision differs (no-op resume vs real upgrade). NOT critical: the stale target is printed in the Plan and the metric keeps reporting 1, so `--source` remains an escape hatch. | fix — have Resolve discover the floating slot tag (align tagPrefix with tagSegments/slotTag), or at minimum document the divergence at tagPrefix and in the upgrade runbook | Reopen if tagPrefix/imageTagBase, DiscoverLatest's matching, or slotTag changes in a round >= 3 |
| F3-7 | `tools/internal/step/upgrade/upgrade_active.go:16-36` (RecordAppliedDigest doc comment); `tools/internal/step/upgrade/verify_active.go:22-32`; `tools/internal/step/upgrade/resolve.go:426-456`, `:497-501`; `tools/internal/workflow/upgrade/upgrade.go:152-166` | 3 | 3 | open | defer | 1 blocking / 2 defer -> defer (blocking-or-higher is 1/3, below half; rule 3 applies). FACT CONFIRMED BY ALL THREE: VerifyActive does exactly two things — the boot-mode probe must return 'active', and a plain string equality (resolve.go:499-501) between `imageRefFromMetadata(imageRepository(plan.Target.Ref), meta)` and `plan.Target.Ref`, built from imageTagBase (:455) which carries flavor/KairosVersion/Arch/Hardware/Role/KubernetesVersion and NO digest and NO sourceCommit. Two builds of the same component versions — precisely the rebuild class this artifact ships — reconstruct an identical string, so the second check has no content and only the boot-mode check discriminates. The new RecordAppliedDigest doc comment ('MUST run only after VerifyActive has confirmed the node actually booted the target image') therefore overstates its own foundation. The blocking voter argued the marker step rests on a guarantee that is not implemented; the two defer voters could not complete the blocking sentence: reaching a wrongly-written marker ALSO requires the boot-mode probe to report 'active' on a node that did not boot the freshly written COS_ACTIVE (Kairos falls back to passive/recovery, which the probe catches), so the operator builds, runs and decides identically today. resolve.go:497-499's own comment already concedes the tag comparison is intentional pre-existing behaviour. NAMED FUTURE COST: the post-reboot metadata carries sourceCommit and the Plan carries TargetSourceCommit, both in hand and neither compared, while the doc comment invites a later maintainer to trust an identity guarantee that does not exist. | defer — compare `meta.SourceCommit` against `plan.TargetSourceCommit` in VerifyActive (both already available), or downgrade the RecordAppliedDigest comment to what Verify actually checks | Reopen if VerifyActive's comparison, imageTagBase, or the RecordAppliedDigest doc comment changes in a round >= 3 |
| F3-2 | `tools/internal/step/upgrade/upgrade_active.go:25-32`; `tools/internal/step/upgrade/resolve.go:272-295`; `tools/internal/client/oci/discover.go:111-145, :180-214`; `tools/internal/workflow/upgrade/upgrade.go:163-166`; test dirs `tools/internal/step/upgrade`, `tools/internal/workflow/upgrade` | 3 | 3 | open | defer | 3/3 defer. Both halves substantiated. DEAD BRANCH: `plan.Target` is assigned only at resolve.go:281/:293 from `imageRef(img)` after an error-checked InspectImage/DiscoverLatest, and discover.go:116-119 returns an error rather than an Image when `d.reg.Digest` fails (DiscoverLatest errors when no candidate survives), so `plan.Target.Digest == ""` is unreachable and the `sudo rm -f <marker>` branch at upgrade_active.go:30-31 cannot fire through `k2-tools upgrade` — while its doc comment presents the removal as a live safety property. TEST GAP: `grep -rn 'AppliedDigest|RecordApplied' tools/` matches no _test.go file, and `go test ./...` reports `? .../workflow/upgrade [no test files]` — the step ORDERING that is the entire round-2 fix (and the subject of F3-1) ships with zero regression coverage, so a future refactor can silently move the write back before Reboot. Nobody is stopped today: the reachable branch is correct and the suite is green. This is the model's own defer shape (coverage not yet written + prose that will mislead a later reader). | defer — add a fake-Remote test asserting the marker command runs only after VerifyActive, and either delete the dead branch or downgrade its comment to a defensive note | Reopen if RecordAppliedDigest's branches, the plan.Target assignment path, or the workflow step order changes in a round >= 3 |
| F3-3 | `tools/internal/workflow/upgrade/upgrade.go:220-227` (`upgradePlanFields`), lines 223 and 226 | 3 | 3 | open | defer | 3/3 defer. Confirmed verbatim: :223 `sequence := "cordon -> drain -> upgrade active -> reboot -> verify -> upgrade recovery -> uncordon"` and :226 `sequence = "verify active -> smoke-check -> upgrade recovery -> uncordon"`, while the registered steps at :126-195 now number NINE including the unconditional `Record applied image digest` at :163-166 (no `.Unless`), so both strings are wrong in both modes; the full-upgrade string also still omits smoke-check, a drift that predates this change. The Sequence KV is rendered into the Plan the operator reads before the Confirm prompt. Not blocking by unanimous agreement: the omitted step is a non-disruptive marker write, the operator answers the y/N prompt identically either way, and the step name prints as it executes. NAMED FUTURE COST: a summary whose own doc comment (:215-218) commits it to being 'boring + factual' has now drifted twice, and each drift makes the next reader trust the line less. | defer — one-line update to each sequence string (add 'record digest', and add the long-missing 'smoke-check' to the full-upgrade string) | Reopen if the workflow step list or `upgradePlanFields` changes in a round >= 3 |
| F3-9 | `kairos/overlays/role/storage/oci/system/oem/10-storage-services.yaml:55` (k2-image-check.timer enable); `kairos/node-agent/internal/imagecheck/imagecheck.go:24, :117-133`; `tools/internal/step/upgrade/resolve.go:259` | 3 | 3 | open | defer | 3/3 defer. Every fact confirmed. The storage overlay now enables k2-image-check.timer hourly; imagecheck.go:117-133 prefers /usr/local/.state/k2-image-digest over the sourceCommit fallback; the ONLY writers of that path are upgrade_active.go:31/:35 behind `k2-tools upgrade`, whose Resolve hard-requires a Kubernetes Node match (resolve.go:259-262 FindNodeByInternalIP, erroring otherwise); and the appliance k2-st-0e12 runs outside Kubernetes, so its marker can only ever be written by hand. docs/design/storage-appliance-checkpoint.private.md:403 records this explicitly as the design ('k2-tools upgrade is CLUSTER-NODE ONLY ... the appliance is outside k8s, so its upgrades are driven by hand'). Live state measured: the appliance carries a hand-written marker sha256:7474dd79... while the slot tag now resolves to sha256:3acfcedd..., so the metric reads upgrade_available 1 — which today is CORRECT. Not blocking: the failure mode of a forgotten manual marker update is a stale digest producing a PERSISTENT, VISIBLE upgrade_available 1 on the very panel the operator watches — loud and self-announcing, remediable by writing one file — so no consumer's next action fails. NAMED FUTURE COST: the appliance's marker-refresh step is booked nowhere in the repo, so panel 30's appliance row will eventually be a standing false alarm that trains the operator to ignore it. | defer — document the `printf | sudo tee /usr/local/.state/k2-image-digest` step in the appliance upgrade runbook, or teach imagecheck to fall back to sourceCommit when the marker disagrees with the running image | Reopen if the appliance gains an automated marker writer, if imagecheck's marker precedence changes, or if the storage overlay's timer set changes in a round >= 3 |
| F3-4 | `kairos/node-agent/internal/metrics/metrics.go:434-452` (`saveConfigInSync`); `kairos/node-agent/internal/health/health.go:58-60, :143-160` (`targetCount`); storage-appliance.json panel 12 | 3 | 3 | open | defer | 1 drop / 2 defer -> defer (>= half at defer-or-higher). THE DIVERGENCE IS REAL: metrics.go:441-443 now returns false on ANY read error where round 2 replaced `os.IsNotExist(err) && liveTargets == 0`, while health.go:143-148 returns (0, nil) for os.IsNotExist and health.go:58-60 calls fail() only on a parse error — two K2 components read the identical defaultSaveConfig path (metrics.go:28 == health.go:18) and now ship opposite verdicts on an absent file. BUT THE ESCALATING CLAIM IS DISPROVEN BY MEASUREMENT, which is why this is not blocking: two voters independently SSHed to the appliance and found `/etc/rtslib-fb-target/saveconfig.json` PRESENT (`-rw------- 1 root root 69 Aug 10 01:00`), so the live `k2_lio_saveconfig_in_sync 1` (scraped alongside k2_lio_targets 0, k2_zfs_volumes 0, k2_storage_healthy 1, no k2_node_build_info — pre-artifact binary) comes from the present-but-EMPTY case, where `len(parsed.Targets) == liveTargets` is still 0 == 0. The value stays 1 under the new code and panel 12 does NOT flip red on rollout; no operator is confronted with a red panel on a healthy box. The drop vote argued the two functions answer different questions; the majority kept it because the file-absent divergence survives that argument. NAMED FUTURE COST: if saveconfig.json is ever removed or renamed, k2_storage_healthy will read 1 next to a red panel 12 with nothing documenting the reconciliation, and the present-but-empty path the appliance actually exercises has no test (TestCollectLIOMissingSaveConfigIsOutOfSync covers only the absent case). | defer — pick one convention deliberately, add a cross-reference comment between metrics.go:434 and health.go:143, and add a present-but-empty test case | Reopen if saveConfigInSync or health.go's targetCount changes, or if anything starts creating/removing saveconfig.json, in a round >= 3 |
| F3-5 | `kairos/node-agent/internal/metrics/metrics.go:434-439` (doc comment) vs `:369-374` (`collectLIO`) and `:390-397` (`countLIO`) | 3 | 3 | open | defer | 3/3 defer. Confirmed by reading the adjacent code. The round-2 comment justifies the new return value with '...and with an unreadable configfs (live count 0) both-absent would otherwise read as a clean bill of health', but an unreadable configfs root NEVER produces live count 0: countLIO (:391-396) returns the non-ENOENT error, collectLIO (:370-374) turns that into `groupResult{success: false}` with a nil samples slice, so k2_lio_saveconfig_in_sync is ABSENT rather than 0. The state the sentence actually describes is a MISSING configfs root, which countLIO maps to (0, 0, 0, nil) with success true. Runtime behaviour is unaffected, so nobody is stopped. NAMED FUTURE COST: this is the written RATIONALE for a behaviour change, sitting on the line round 2 just rewrote, so the next maintainer to revisit saveConfigInSync reasons from a false model of its sibling collector's failure path. | defer — one-word fix: 'unreadable' -> 'absent' (or spell out that an unreadable configfs emits no LIO samples at all) | Reopen if the saveConfigInSync comment or countLIO/collectLIO error handling changes in a round >= 3 |
| F3-6 | `kairos/node-agent/internal/imagecheck/imagecheck.go:135-151, :196-233, :247-276` | 3 | 3 | settled | verified | 3/3 verified — an AFFIRMATIVE result, independently re-measured against the real registry by two voters using the code's exact request shapes rather than accepting the reviewer transcript. Anonymous token from `https://ghcr.io/token?scope=repository:wyvernzora/k2-kairos:pull` succeeds; the slot-tag manifest request with imagecheck's three-type Accept header returns 200, content-type application/vnd.oci.image.index.v1+json, docker-content-digest sha256:3acfcedd..., with NO top-level `config` key so fetchRevision takes the index branch (:196-233), and exactly TWO descriptors — sha256:8977d617 platform linux/amd64 and sha256:6e8f5fab platform unknown/unknown annotated vnd.docker.reference.type=attestation-manifest — so the `len(matches) != 1` guard at :208-210 passes on the real production shape (the assumption most likely to rot). The referenced config blob is 26130 bytes, well under the 1 MiB maxResponseBytes, and carries `org.opencontainers.image.revision` = 430ccfbf825309c865179e0f0bdbbece74716306 alongside the io.k2.* labels. The blob GET 307-redirects to pkg-containers.githubusercontent.com and Go drops Authorization on the cross-host redirect, which is HARMLESS — the presigned location returns the same 26130 bytes with no auth. Full 5-request sequence measured 1.07 s against the 30 s overallTimeout (imagecheck.go:29). Corroborates settled F0-20/F0-25/F0-26. | verified — no action; the marker-absent fallback works end-to-end against live GHCR inside its timeout budget | Reopen if the manifest traversal, the platform-match guard, the Accept header set, or overallTimeout changes in a round >= 3 |

## Round 0 outcome

- critical: 0
- blocking: 3 — F0-1 (node-exporter filesystem excludes replaced), F0-2 (Prometheus default-deny egress), F0-3 (panel 15 absence detector inert)
- defer: 14 · advisory: 3 · verified: 11 · drop: 0

Carried operational caveat (from F0-30): `earthly +lint` and `earthly +k8s-manifests` have not been executed against
this working tree. Run both before commit.


## Round 3 outcome

- critical: 0
- blocking: 2 — F3-1 (round-2 marker move defeats resume-post-upgrade for every marker-bearing node), F3-8 (upgrade auto-discovery searches version-qualified tags while the new node-side image-check watches the version-free slot tag)
- defer: 7 · advisory: 0 · verified: 4 · drop: 0

Round-2 blockers F2-1 and F2-7 are both DISCHARGED and settled. The recurring dashboard title/colour over-claim class is exhausted
(panel-shape audit across every data panel found no remaining guarantee beyond its producer). Both round-3 blockers are in
`tools/internal/step/upgrade` / `tools/internal/workflow/upgrade` — the upgrade CLI, not the metrics or dashboard half — and F3-1 is a
regression introduced BY the round-2 fix, so re-test the resume path explicitly after the next revision.

Carried operational caveat: `earthly +lint` and `earthly +k8s-manifests` passed against the round-2 tree (F0-30); re-run both after
the F3-1/F3-8 fixes land.